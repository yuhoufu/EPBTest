##### 数据落盘的逻辑：

###### 共有12个EPB卡钳，6个一组，每组一个压力，夹紧力固定在数采2；

现在软件的需求是：

1. 每次停止试验，每个卡钳只保留最新的10组（最新10次计数）数据，最好是这个参数是可设定的，可以是存储最近具体计数的数据，也可以是按原来的逻辑一直存；

2. 每个卡钳的进度可能不一样，有的卡钳可能会中途就停止，比如一组卡钳6个，本来预计的次数是10000次，但是卡钳1在5000次就停止了，那么卡钳1存储的就是4991到5000次的数据，其他的都是9991到10000的数据；

   

3. 其他的逻辑和之前一样；

