# 3.0.0.0 完整包交付与现场切换

## 当前部署判断

以c8daa03候选包为准，JXCQ安装、空闲启动/正常关闭、观察任务、回退，以及现场配置和历史索引的离线兼容预核验已有通过证据。可提交用户安排维护窗口，进入**受控的现场升级观察与验收**；尚未批准直接恢复正式带载试验或开放自动恢复。窗口内仍须最新一致备份、计数对账、正常停机后的安全确认及实际硬件验证。主程序需要配套升级。未明确窗口前保持wj-epb现有程序。

最新独立性补测：同一c8daa03包在JXCQ注册独立SYSTEM验收任务，业务EXE路径不存在且无运行授权时，两类任务各执行两次，共4次通过；无恢复请求，验收任务已清理，旧Supervisor保持Running、主程序0。证据artifacts/ReleaseValidation-20260908/jxcq-c8daa03-independent-task.json。该结果证明缺失业务目录下的独立运行，不扩大为正常带载试验中退出Guard的无影响验收。

下文按时间保留早期“尚未安装/尚未回退”等记录，当前状态以本节及后续时间补录为准。维护窗口已再次询问用户，未获得具体时间前不执行现场切换。

日期：2026-09-08。目标是先完成 JXCQ 无硬件软件验证，再交付 wj-epb 可安装的完整程序包。本文不授权立即中断 wj-epb 的正式试验。

**15:55 更新：当前冻结包不作为现场切换放行包。** JXCQ 真实安装预演发现 Windows PowerShell 5.1 下的 WhatIf 哈希校验缺陷，源码已修复并通过本地41项回归及远程预演，但修复尚未进入该ZIP。完整安装、启动、回退及现场原项目兼容验证仍未完成。以下哈希与结果保留为历史候选包证据；须重新构建并完成准入后交付替代包。

## c8daa03 修复候选包

2026-09-08 16:21回退补录：JXCQ主程序正常退出后，随包Guard卸载成功；监督服务及会话代理退出后，从本轮已核验备份恢复2.17.3.0，120个原安装文件SHA256全部一致。恢复原配置、项目选择和三项任务XML；保留新程序槽及本轮ProgramData/LocalAppData状态供审计，未回放旧运行授权。回退后Supervisor Running、主程序0、Guard任务0。证据artifacts/ReleaseValidation-20260908/jxcq-c8daa03-rollback-result.json。至此本轮JXCQ完整包实际安装、空闲界面启动/正常关闭、观察任务及程序回退均有实测证据；仍不等于现场2.14.2.11原项目跨版本兼容、带载续测或自动恢复验收。早先“回退待完成”表述是相应时间点的历史状态。

2026-09-08 16:19启动补录：JXCQ使用独立IdleStartup项目、全部通道禁用，经监督入口实际启动新主程序。第一次测试准备文件带UTF-8 BOM，被项目选择反序列化拒绝并显示提示，不能算通过；确认提示后正常关闭，未强杀。改用无BOM文件后，第二次PID22820主窗口标题3.0.0.0、响应正常、窗口与菜单Enabled=true且无模态提示；随后发送正常关闭请求。证据artifacts/ReleaseValidation-20260908/jxcq-c8daa03-ui-retry.json及jxcq-c8daa03-ui-close-final.json。范围仅为完整生产程序空闲启动/关闭，不是模拟采集、硬件输出或续测验收。回退及现场原项目兼容仍待完成。

完整Build-Release已退出0，源码提交c8daa03035561465e4cb07903469c22d34efe882、工作树干净。控制801/801、写盘70/70、电源19/19、现场门禁49项、隔离部署41/41通过。匹配的Guard观察包与Main共享组件一致，整包只读校验通过。日志位于artifacts/ReleaseValidation-20260908/build-formal-whatif-fixed.log及bundle-c8daa03-verify.log。

新候选为`artifacts/MTTFTest-3.0.0.0-WjEpb-FieldObservation-c8daa03-20260908.zip`，48,786,229字节，SHA256为`789773AE6725D7C5058B69C12193020BE6BF8E91D15D1070957D8D1DF0B70F9E`。包含WhatIf修复及同提交Guard，旧ZIP未覆盖。新包仍是待验收候选：实际安装、启动、回退及现场原项目兼容验证未完成，自动执行保持禁用，不能据此切换wj-epb。

## 交付范围

2026-09-08 16:12补录：c8daa03包在JXCQ完成实际Main安装，安装器退出0，安装版本3.0.0.0，Supervisor与SessionAgent运行；随后配套Guard安装退出0，观察任务手动触发后LastTaskResult=0，执行任务Disabled。证据为artifacts/ReleaseValidation-20260908/jxcq-c8daa03-actual-install.json及jxcq-c8daa03-guard-install.json。主程序尚未启动，不能记作启动或业务验收。JXCQ默认项目仍指向D:\EPB_Data\10358-029，后续启动前必须隔离旧项目及恢复状态。安装前程序、停服后的ProgramData和LocalAppData备份及哈希保存在C:\RecoveryGuard-3.0-Validation\FinalPackage-Install-20260908；实际回退尚未执行。首次收口检查遇到代理退出延迟而中止，复核所有相关进程已退出后才继续安装，未强杀。现场wj-epb未改动。

2026-09-08 16:09补录：上述c8daa03新候选已传至JXCQ，远端ZIP摘要匹配，解压后的Verify-Bundle退出0，随包原始Main安装器-WhatIf退出0；这次未使用外置修复脚本。证据artifacts/ReleaseValidation-20260908/jxcq-c8daa03-package-preflight.json。尚未调用实际安装入口。

完整包由 Main（3.0.0.0 主程序、Supervisor、SessionAgent、安全组件、运行依赖、安装及校验脚本）和 Guard（独立观察检查器及安装脚本）组成。Guard 使用 ObserveOnlyCommissioning，执行任务禁用。主程序与 Guard 配套升级，不能把新 Guard 直接安装到旧版主程序上。

主程序正式构建已通过发布校验：FORMAL_RELEASE、deploymentApproved=true、gitDirty=false。源码提交为 c795442ab12048ba3ad9f7b80075392a2045cbd7，位于独立分支 codex/release-v3.0.0.0-20260908；原工作区保留未提交修改。这里的正式构建批准不代表现场硬件验收或自动恢复批准。

交付名为 MTTFTest-3.0.0.0-WjEpb-FieldObservation-20260908.zip，外部同名 .sha256 文件记录压缩包校验值。包内 Verify-Bundle.ps1 只读核对完整文件集合、SHA256、主程序正式身份、安装器准入及 Guard 核心组件一致性，不安装或启动程序。使用 Windows PowerShell 5.1 运行该校验脚本；若执行策略阻止加载，可对本次已核验脚本进程使用 powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\Verify-Bundle.ps1，不修改全局执行策略。

本次正式构建通过控制801/801、写盘70/70、电源19/19、现场门禁脚本49项、隔离换包39/39及部署契约检查。主程序 SHA256 为 433A039933612616C20071C866181B0EA77A321CDCA618A7B5BE081C6C41B707。最终 JXCQ 整包只读复核及证据以交付记录为准。

## JXCQ 验证边界

最终完整包已在 JXCQ 通过只读校验和包内组件恢复接入39/39，退出码均为0。校验同时验证实际安装器接受 Main 的正式身份；未调用安装/启动服务的入口。最终证据为 artifacts/ReleaseValidation-20260908/jxcq-delivery-result.json，远程目录为 C:\RecoveryGuard-3.0-Validation\Delivery-20260908-c795442。

交付文件位于 artifacts/MTTFTest-3.0.0.0-WjEpb-FieldObservation-20260908.zip，大小48,722,287字节，SHA256为 **6E730D68F7116C13C338573DB21FF739FCD42F35406F802EF68EFAFA412C205E**。同名.zip.sha256提供外部校验值。压缩包已冻结，本文后续补录的远程验收结果不会回写并改变压缩包。

结论：本次已覆盖的软件模拟、完整包身份与安装准入未发现尚未修复的部署阻塞，可以通知用户安排 wj-epb 维护窗口升级观察。尚未在 JXCQ 安装运行这份完整生产主程序，也未验证真实硬件控制；不能据此报告整机带载或自动恢复验收通过。JXCQ 最终状态为 Supervisor Running、主程序0、Guard任务0。wj-epb 未连接、未停机、未换包。

已有证据包括 Guard 独立契约回归 91/91、真实 SYSTEM 扫描/执行任务的无硬件恢复链 10/10、安装/升级/注入回退/卸载 4/4。2026-09-08 新增恢复接入 39/39 和六通道每通道 2 kHz、60 秒模拟采集的真实写盘验证；以实际日志为准，不累计为长期整机试验。

JXCQ 另有隔离换包事务专项37/37。完整39项套件中的两个权限注入用例在该远程会话不能制造预期权限拒绝，保留失败记录并单独标注环境限制；它们未被算作远程通过。本地完整39项通过。详情见[无硬件模拟复核](02_Issues/2026-09-08_JXCQ_无硬件模拟复核.md)。

这些测试不连接现场硬件，不检测 CAN，也不证明 NI、电源或液压的物理断能。I0050 液压恢复状态发布一致性的根因修复仍属于第二步。当前完整包用于维护窗口升级后的现场运行观察，不能宣称已经消除 I0050 或开放现场自动恢复。

## wj-epb 切换顺序

### 2026-09-08 15:51 部署就绪复核

当前结论是**交付包已生成，软件层验证通过，但尚未确认可以切换 wj-epb 正式试验**。前文“可以通知用户安排维护窗口升级观察”仅表示候选包可进入现场准入准备，不能视为部署放行；目前不应要求用户先停止正式试验。

本次仅只读查询进程、服务、磁盘余量和任务元数据：wj-epb 实际主机 MT-20250724OGLX，安装版本 2.14.2.11，主程序 PID 20744，自 2026-09-07 18:58:59 +08:00 启动；Supervisor Running，无 RecoveryGuard 任务。C 盘剩余约 8.24 GiB，D 盘约 192.44 GiB。这些信息只证明组件存在及磁盘余量，不证明试验业务健康或升级兼容。证据：[只读现场快照](../artifacts/ReleaseValidation-20260908/wj-readiness-readonly.json)。未停止试验、替换文件或操作硬件。

部署放行前仍需补齐：在 JXCQ 对最终完整包执行实际安装、无硬件启动和回退验证；针对现场 2.14.2.11 原项目的隔离副本核验配置、检查点、数据与剩余圈数的跨版本兼容；核对现场实际依赖及形成可执行的备份与回退记录。无硬件测试不能代替现场 NI、电源、液压验收。主程序必须配套升级，Guard 首期仅观察；I0050 修复仍在第二步，自动恢复未获放行。

1. 通知用户软件验证和完整包已就绪后，再安排维护窗口。正在进行的正式试验期间不覆盖文件、不重启服务、不安装 Guard。
2. 窗口开始时重新只读核对实际运行版本、安装路径、活动项目及各通道目标/完成/剩余圈数；不能用历史部署记录代替现场快照。
3. 通过正常业务停止保存试验，核对写盘完成和现场设备安全状态；备份原完整安装目录、ProgramData 配置/恢复状态、项目数据及服务/任务定义。
4. 校验完整包 SHA256、Main 内 build-identity.json 和 SHA256SUMS.txt，以及 Guard 内 guard-identity.json。执行 Main/Deployment/Verify-Release.ps1 的只读校验；Guard 使用 Install-MTTFTest-RecoveryGuard.ps1 -Mode Validate。
5. 使用 Main 随包安装器升级同一实际安装根目录，保留现场配置。安装器若要求旧检查点安全迁移/断能证明，必须按真实现场状态处理；不预填 PhysicalIsolationConfirmed 以绕过门禁。
6. 主程序仍未启动、旧看门狗会话已收口时，再安装独立 Guard，绑定真实台架标识和升级后的主程序路径。确认观察任务正常、执行任务禁用，然后使用随包入口启动主程序。
7. 核对原项目、通道隔离状态和剩余圈数，由现场确认继续试验。监控采集进展、逐通道业务提交、圈数和恢复日志；不以进程存活代替业务持续运行。

## 回退原则

发现升级后影响业务或数据不一致时，先正常停止并保存新版本产生的记录，保留日志和检查点供对账。使用原包和已核验备份恢复安装组件，回退前对账升级后新增圈数。不得直接用旧 ProgramData/项目备份覆盖新数据，导致圈数倒退或旧授权复活。恢复正式试验前再次核对配置、通道与剩余目标。

现场操作遵循 docs/现场远程部署与运行监控章程.md 和 docs/由codex远程监控项目运行.md；当前程序无 CAN 硬件的范围约束优先于历史文档中的相关遗留表述。
