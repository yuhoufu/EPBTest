# wj-epb 恢复修复候选包验证记录

## 交付结论

2026-09-10 本地修复、回归及候选包生成完成。现场没有部署本候选、没有重置圈计数、没有操作现场硬件。候选不能等同于现场稳定性验收通过。

分析依据见 [短时全停与恢复授权断链分析](2026-09-10_wj-epb_V3短时全停与恢复授权断链分析.md)。已修复学习重试终态过晚、恢复清场误撤授权、启动尾声另建停止代次导致原清场失效，并补上人工停止合并到恢复事务时的同步撤权保护。压力、电流、持久化、安全截止和人工停止门禁均保留。

## 源码与安装包身份

- 独立分支：`codex/fix-wj-v3-recovery-20260910`。未覆盖主工作区已有源码修改。
- 独立工作区：`C:/Users/19812/Documents/Codex/2026-09-10/epb-recoveryguard-handoff/work/bundle-source-0f28efe`。
- Main/Guard 组件提交：`6a1698becaeb2c047dd67dbfb6e9266e97be3979`。
- 打包工具提交：`127b2cb94156c9ad12106e42c475a6eb7bcccb7e`。仅增加从已校验Base直接生成一键包的入口；已验证组件二进制未改变。
- 主程序版本：`3.0.0.0`；EXE SHA256：`79395683B0D930B72130B6B92BEAF0194EB299B0E8CE541919B4A181EDF2A2CF`。
- [候选包目录](../artifacts/deploy/V3.0.0.0_127b2cb94156_AUTO_RECOVERY_ONECLICK)。
- [候选安装包](../artifacts/deploy/V3.0.0.0_127b2cb94156_AUTO_RECOVERY_ONECLICK.7z)，34,643,778字节（约33.04 MiB）。
- 压缩包 SHA256：`08699C8CC9BD21C0351CE23FEADBC1624358C43CDFF59FEF5C7A43A074713159`。

外层与Base直接子项名称已同 `V3.0.0.0_783bd71ecb38_AUTO_RECOVERY_ONECLICK` 比较，完全一致。保留Base/Package、Base/Guard、内外一键安装/修复及状态脚本；不包含现场增量数据、日志或dump。压缩体积主要来自程序及UI等运行依赖，未通过删除运行依赖追求更小体积。

## 验证结果

| 检查 | 结果 |
| --- | --- |
| 最终组件Debug x86解决方案构建 | 通过 |
| 最终组件Release重建 | 通过，0错误；存在编译警告，未称零警告 |
| Release AdaptiveControlTests完整回归 | 989/989 |
| Release EpbDiskWriterTests | 76/76 |
| Release PowerSupplyDebugger.Tests | 19/19 |
| RecoveryGuardTests | Debug及Release均101/101；最终补丁未改Guard/共享恢复库源码 |
| 最终组件恢复生产协调器专项 | 50/50 |
| 最终组件真实StopAll生产链路 | Debug、Release均16/16 |
| 现场证据判定器 | 49项通过；包含数据未推进、授权及恢复链判定 |
| DAQ间断去重统计脚本 | 2/2，包含阈值、重复事件、时间窗边界及缺失窗口拒绝 |
| 部署契约/回滚 | 发布脚本全部通过；部署43/43、安装回滚4/4、事务恢复3/3、Current槽16/16等 |
| 一键包入口及防篡改 | 6/6；无需传验收报告，不修改本机服务/任务/硬件 |
| 压缩及交付回读 | 7-Zip测试通过；解压后ValidatePackage通过；SHA256与旁置校验文件一致 |

完整发布测试摘要保存在 [Main构建身份](../artifacts/releases/wj-recovery-fix-20260910/V3.0.0.0-6a1698becaeb-20260910_090945/build-identity.json) 的verification字段。`FORMAL_RELEASE`、`deploymentApproved`是构建流程标记，不是本次现场验收结论；其中fieldValidation仍为待现场状态。一键包的`fieldAcceptanceRequired=false`只表示安装入口不强制报告参数，`hardwareValidationPerformed=false`仍如实保留。

原始复现及专项日志见 [验证证据目录](../artifacts/recovery-fix-validation-20260910)。其中修复前复现日志显示 `Generation=1, CurrentGeneration=2, LogicalPending=True, HardRestartLatch=False`；修复后相同真实停止/生命周期链路允许逻辑收敛。测试中只替换硬件端口，并不连接现场NI/液压/电源。

最终代码可复验的命令（在独立工作区执行）：

```powershell
msbuild TfTest.sln /p:Configuration=Debug /p:Platform=x86
.\Tests\AdaptiveControlTests\bin\Release\AdaptiveControlTests.exe
.\Tests\AdaptiveControlTests\bin\Release\AdaptiveControlTests.exe --stop-production-seam
.\Tests\AdaptiveControlTests\bin\Debug\AdaptiveControlTests.exe --recovery-production-seam
.\Tests\EpbDiskWriterTests\bin\Release\EpbDiskWriterTests.exe
dotnet test .\Tests\PowerSupplyDebugger.Tests\PowerSupplyDebugger.Tests.csproj --configuration Release
.\Tests\RecoveryGuardTests\bin\Release\RecoveryGuardTests.exe
.\Tools\Test-DaqGapEvents.ps1
```

构建过程曾因构建与测试同时占用文件、Debug/Release还原目标差异重试；最终构建及专项均已重新通过。较早组件提交的发布流程在最终源码变化后被源码身份保护拒绝，未交付混合版本。旧自动中间包生成器与当前安装器的schema不兼容，已改用经过完整校验的Base直出一键包；失败中间产物只保留在验证目录，不作为交付。

## 尚未验证与现场边界

1. 底层DAQ间断频率为何变化仍未确定。旧版正式运行窗口与新版不足2分钟的学习窗口不可直接外推版本故障率；已记录独立事件数、观察时长和阶段，未调整安全超时阈值。
2. 未完成本候选的现场NI/液压/电源故障注入和长稳；本地模拟通过不证明软件已优于2.14.2.11。
3. 现场验收需另行授权窗口：学习/正式阶段分别测试单通道、部分通道和全部通道可恢复故障；以动作恢复、计数递增、Raw/SQLite耐久提交推进作为通过条件，并记录耗时。
4. 同进程清场确实失败时，仍需验证真实机器的安全确认→恢复许可→旧进程退出→新进程准入→实际续测全链路。人工停止必须阻止重启；物理安全或数据边界未确认时必须保持停止。
5. 不自动升级wj-epb，不自动重开之前的监控任务；现场继续保留旧版运行安排。
