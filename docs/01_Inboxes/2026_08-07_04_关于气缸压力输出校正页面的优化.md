# 关于气缸压力输出校正页面的优化

![image-20260807170044319](D:\Github\wanxiang\EPBTest\docs\01_Inboxes\assets\image-20260807170044319.png)

这是界面存在的问题，如果还有类似的我没发现的，一并美化；

## 工控机的显示器参数：

缩放与布局 ：100% (推荐)

显示分辨率 ：1440×900 (推荐)

![image-20260807181203605](D:\Github\wanxiang\EPBTest\docs\01_Inboxes\assets\image-20260807181203605.png)

## 开发机的显示器参数

![image-20260809203043692](D:\Github\wanxiang\EPBTest\docs\01_Inboxes\assets\image-20260809203043692.png)

## 2026-08-09 实施记录

### 完成情况

本次修改仅调整 `MTTfTest/FrmTestSetting.PressureCalibration.cs` 中气缸压力输出校正页的布局和控件样式，没有修改压力校正事件处理、硬件输出协调器、DAQ 采样、试验状态机或其他试验逻辑。

| 原图标注 | 落地结果 |
| --- | --- |
| 1. 气缸选择显示不完整 | 扩大气缸下拉框及所在列宽，统一使用 `Microsoft YaHei UI` 中文界面字体，`Cylinder1`、`Cylinder2` 可完整显示。 |
| 2. “使用实时值”显示不完整 | 增大按钮安全宽度，并改为蓝色描边的次要操作样式，文字完整且与主操作区分明确。 |
| 3. 底部操作文字显示不完整 | 重新分配“记录当前点 / 删除选中点 / 清空 / 保存校正”的列宽，增大操作行高度；按钮文字均可完整显示。 |
| 4. 底部状态文字显示不完整 | 页脚由 118 px 增加到 148 px，公式行和状态行分别获得不小于控件最小高度的空间，并增加左右内边距。 |
| 5. 按钮文字拥挤、满色按钮过多 | 建立主操作、次要操作、危险操作三种视觉层级：开始/保存使用实心主按钮，普通操作使用蓝色描边按钮，停止/删除使用红色描边按钮；补齐悬停、按下状态。 |
| 6. 默认空白行及选中行与表头混为一体 | 保留 `AllowUserToAddRows = false`，不显示 DataGridView 默认新增空白行；配置中已有的历史校正点仍正常显示。隐藏无业务含义的行头，将选中行改为浅蓝底深色字，和深蓝表头清晰区分。 |
| 类似问题 | 缩小无效外边距、统一文字颜色和中文字体；表头、数据行高度改为紧凑的数据页规格；网格线改为浅灰蓝色，提升表格可读性。 |

### 显示适配验证

- 按工控机目标环境对应的窗体尺寸 `1415×780` 进行实窗渲染检查，可容纳于 `1440×900、100%` 显示环境。
- 气缸下拉框、命令压力、实时压力、实测压力、顶部操作按钮、底部操作按钮、当前公式和状态提示均完整显示。
- 页面没有横向滚动条，表格和页脚之间没有遮挡。

### 构建与回归验证

- `MTTfTest/MTTfTest.csproj`：`Debug|AnyCPU` 构建成功，0 个错误；现有 41 个编译警告与本次页面修改无关。
- 压力校正专项回归：`CalibrationMathTests` 8/8 通过，覆盖多点拟合、气缸映射、120 bar 限幅、DO/AO 安全输出顺序、采样过期回零、超压回零、后台看门狗和配置原子保存。
- 全量 `AdaptiveControlTests` 在大量既有用例通过后，停在既有 DAQ 后台任务收口用例 `DaqBackgroundTasksAreCoalescedAndDrained`；失败点不在本次修改文件或压力校正链路内。
