### 事项1

- 剩余 EPB8、EPB11 于 23:41:30 开始同一周期。EPB8 在错峰 800 ms 到来之前异常提前结束正向；EPB11 启动时又把 EPB8 登记回液压 `InFlight` 集合。之后 EPB11 到释放点，但集合中残留 EPB8，统一释压条件永远不能成立。
- 两个周期分别保持在 `running` 约 62 分钟，直到 2026-08-03 00:43:35 收到“停止全部 EPB”请求才被取消。

1. 针对上述请求，目前程序没有相对的异常处理吗？没有的话，该增加怎样的异常处理； 
2. “两个周期分别保持在 `running` 约 62 分钟”，此时卡钳是断电状态吗？出现异常卡钳首先就得断电，保护试件； 
3. “EPB8 在错峰 800 ms 到来之前异常提前结束正向”，是什么原因，和程序逻辑有关吗？
4. “62 分钟”内，液压都是保持高压状态吗？
5. 最后如果当前的逻辑存在问题，我希望给我一个更合理的，容错、即时报警的逻辑；

### 事项2

所有的`stopall`都是人工的手动停止记录，后续日志中，可以完善一下，停止动作的产生；



### 事项3

1. EPB8, `启动初期两次预释放失败：反向空行程未在预算内确认；之后学习成功。` 在刚开始试验时，多次启动失败，是在我多次后，才正常启动；
   针对刚接入台架的卡钳，如果卡钳出现类似的问题，能否在自学习的过程中自适应，解决，避免人工过多介入，“多次失败”，会对用户造成不好的体验；

2. EPB9，“学习阶段即出现 `ForwardLoadRiseNotStarted`，”，这个问题有可能是面板上的对应epb通道的按钮未按下，如果检测到电源是正常的，极有可能就是按钮未按下，前提是用户已经把电路接上了，可以弹框提示；

3. 峰值超调

   ```
   EPB9,
   08-01 21:44:04，圈 7151：连续 3 圈正向峰值超调，最终 Peak=15.841 A，Target=15 A，触发 `ForwardPeakOvershoot`
   ```

   出现超调的问题，程序能针对性的调整断电时机吗？

   ```
   00:44、00:46、00:57、00:58 多次单独/批量重启均在学习阶段失败：约 2598～2604 ms 后电流仍只有 0.661～0.680 A，无法开始负载上升。
   ```

    在这个阶段，其他两个正常的卡钳也不能启动，是为什么？

4. EPB10

   ```
   正式完成 1197 圈，累计 4 次同型硬故障，均为 `ForwardCurrentRiseStalled`：
   
   - 07-31 16:31:50，圈 164，Peak=14.051 A；
   - 07-31 18:03:49，圈 340，Peak=13.855 A；
   - 07-31 22:19:49，圈 766，Peak=14.038 A；
   - 08-02 03:13:19，圈 1201，Peak=14.138 A。
   ```

   我记得最后一版，我设定的逻辑是，5次之后报警，但是没出现一次，都得有最新10圈的数据记录；

   这个实现了吗？我未看到具体数据；

5. EPB 11

   
