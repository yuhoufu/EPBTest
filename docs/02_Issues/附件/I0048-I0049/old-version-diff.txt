diff --git a/Controller/ChannelExecutionFence.cs b/Controller/ChannelExecutionFence.cs
index e251d4c..9f0aab5 100644
--- a/Controller/ChannelExecutionFence.cs
+++ b/Controller/ChannelExecutionFence.cs
@@ -83,6 +83,21 @@ namespace Controller
             }
         }
 
+        internal bool RevokeIfCurrent(ChannelExecutionPermit permit)
+        {
+            if (permit.Channel < 1 || permit.Channel > 12) return false;
+            var entry = _entries.GetOrAdd(permit.Channel, _ => new Entry());
+            lock (entry.Gate)
+            {
+                if (!entry.Authorized || entry.Generation != permit.Generation ||
+                    entry.RunEpoch != permit.RunEpoch) return false;
+                entry.Generation++;
+                entry.Authorized = false;
+                try { entry.Revocation.Cancel(); } catch { }
+                return true;
+            }
+        }
+
         internal bool IsCurrent(ChannelExecutionPermit permit)
         {
             if (!permit.Authorized || permit.Channel < 1 || permit.Channel > 12) return false;
diff --git a/MTTfTest/App.config b/MTTfTest/App.config
index a597ab5..0e9dd5d 100644
--- a/MTTfTest/App.config
+++ b/MTTfTest/App.config
@@ -108,7 +108,7 @@
   <add key="DaqControlQueueCapacity" value="64" />
   <add key="DaqControlWarningAgeMs" value="50" />
   <add key="DaqControlHardFaultAgeMs" value="100" />
-  <!-- DAQ存活三级策略：100ms仅Warn，1000ms进入Suspect，2000ms且连续3次确认才局部恢复。 -->
+  <!-- DAQ存活三级策略：75ms预警，100ms禁止样本参与控制，250ms立即断能并作废当前圈。 -->
   <add key="DaqLivenessWatchdogIntervalMs" value="20" />
   <add key="DaqLivenessWarnThresholdMs" value="250" />
   <add key="DaqLivenessSuspectThresholdMs" value="1500" />

2.14.2.11 EpbManager.cs:2066-2080
2066:         private void RequestFormalParticipantRetirement(
2067:             FormalBatchParticipantLease lease,
2068:             string reason)
2069:         {
2070:             if (lease == null) return;
2071:             if (!_formalBatchSlots.RequestRetirement(lease, reason)) return;
2072:             ObserveSafetyTask(
2073:                 CompleteFormalParticipantRetirementFenceAsync(
2074:                     lease,
2075:                     reason),
2076:                 "FormalParticipantRetirementFence",
2077:                 lease.Channel);
2078:         }
2079: 
2080:         private async Task CompleteFormalParticipantRetirementFenceAsync(
