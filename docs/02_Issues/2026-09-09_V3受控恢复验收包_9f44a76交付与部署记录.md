# V3.0.0.0 受控恢复验收包 9f44a76：交付与部署记录

## 当前结论

完整包已生成，同一份7z已通过JXCQ安装生命周期验证。它新增有界、一次性的受控恢复验收入口，常驻Guard仍默认ObserveOnly、执行任务Disabled；不是已经通过真实硬件验收的自动恢复包。最终目标尚未完成，下一步为现场受控恢复验收及自动包晋级。

## 包身份

- 源码：`9f44a769addeceb6dc99d90007ce31fc130eadf2`，独立发布源码树干净。
- 安装包：`D:\Github\wanxiang\EPBTest\artifacts\deploy\V3.0.0.0_正式版_9f44a769adde_QUICKDEPLOY_GUARD.7z`
- SHA256：`333BF7F865D2E447357C96021785D264EEBD78F23EA2BD69486DAFDD2CBB3F37`
- 大小：34,320,421字节；142文件；7-Zip最大压缩率，压缩后7z t通过。
- Main、Guard、RecoveryControl版本均3.0.0.0；Main和Guard使用完全相同的共享DLL。
- Main SHA256：`3A09D8A7594EA463557B7C211EB4833E14C1EB1C1F72C89D2C2D1C0124DC5C8D`
- Guard SHA256：`BF9CC4D450044DFFC155903A746C338418059820077556C0EB015522F81AC9DA`
- RecoveryControl SHA256：`CA24E7FC3359CDC5C15B8737123B9B8EB47D02496CAC4F2225048BED61ED8A7D`

完整解压后使用根目录的一键安装正式版、启动试验、检查运行状态、一键修复、恢复后台服务、一键故障采证、一键停止全部相关进程、一键卸载CMD入口。保留Supervisor启动准入；不直接双击Main EXE代替监督启动。安装仍遵守停止、安全和数据保留检查。

## 本轮变化与验证

在之前Main整改基础上增加受控验收范围：绑定机器、安装身份、当前试验授权版本及组件哈希；默认10分钟、最多15分钟，从计划生成起计算；每份计划只能执行一次。只允许RecoverExited，人工停止、授权变化、到期和晚到结果都会阻止继续推进。常驻自动模式仍要求真实现场报告。

修复Guard命令行JSON中文路径受Windows码页损坏的问题，输出使用Unicode转义。新增验收脚本及其校验器纳入包清单和源码快照，自动晋级兼容旧7文件包与新9文件包，继续绑定验收时的全部基包哈希。

本次发布流水线：控制803/803、磁盘74/74、电源19/19、现场门禁49项、部署契约41/41通过。包内实际Guard EXE和Main共享DLL完整回归97/97，其中连续失败300次、3589步；JXCQ使用相同哈希组件，期限/授权/晚到结果定向检查3/3。源码快照、包结构和自动准入结果见构建日志；合成报告验证不作为物理安全证明。

## JXCQ 同包实测（北京时间2026-09-09）

| 操作 | 时间 | 结果 |
| --- | --- | --- |
| 一键安装正式版 | 01:25:14 | 退出码0 |
| 检查运行状态 | 01:25:17 | 退出码0 |
| 一键修复 | 01:25:35 | 退出码0 |
| 恢复后台服务 | 01:25:38 | 退出码0 |
| 一键故障采证 | 01:25:42 | 退出码0 |
| 监督启动 | 01:26:10 | 退出码0；PID18592，窗口显示V3.0.0.0 |
| 正常关闭 | 同轮交互验证 | CloseMainWindow=true、Exited=true |
| 停止全部相关进程 | 01:28:42 | 退出码0 |
| 一键卸载 | 01:28:49 | 退出码0 |
| 回退 | 01:28:55 | 恢复2.17.3.0；主程序与8份配置哈希和测试前一致 |

使用路径 `C:\RecoveryGuard-3.0-Validation\批次修复 验证-9f44a76\正式 安装包`。CMD由管理员WinRM及交互桌面任务执行，不声称人工双击/UAC点击验收。回退后MainCount=0、Supervisor Running、Guard任务为空，临时交互任务已删除。

真实包的受控验收Plan在无运行授权时返回 `CommissioningRequiresExistingRunIntent`，退出码非0且未生成计划目录。首次采集被WinRM的原生错误传播打断；改用直接进程输出采集后重跑只读Plan取得完整结果，未发起恢复。

本地证据：`artifacts/field-deploy-20260908-v3-round4/jxcq-9f44a76/`，以 `verified-summary.json`、`actions.jsonl`、`interactive-result.json`、`rollback-result.json`、`commissioning-unarmed-result.json`为准。构建日志 `build-final-9f44a76.log`、最终Guard回归 `test-final-9f44a76-guard.log`。

## 现场及未完成项

wj-epb 01:17的只读快照显示旧版2.14.2.11六通道均Running，未出现读取错误。本轮未停止或替换现场程序。最新包开始传至 `D:\debug\CodexDeploy_20260909_V3_R5\package.7z`，传输句柄31571；须等待成功并校验后才视为预置完成。

仍需完成本次安全停止/保存证据、仅增量封存、现场匹配Main与Guard安装、真实恢复及各参与通道持久化提交对账，之后才能生成和部署自动恢复包。异常优先回退旧版，保留最新数据库、圈数和Current\DataStore，不用旧数据覆盖新数据。不能把本次JXCQ无硬件验证冒充真实恢复或物理安全通过。

当前项目无CAN硬件，不检测CAN，也不作为任何门禁。I0050液压恢复状态发布一致性独立修复仍留第二步。

### R5现场预置与回退准备

传输31571已退出码0，现场SHA256与上述包一致；已解压到R5的Bundle目录并通过包内校验。回退准备目录为 `D:\debug\CodexDeploy_20260909_V3_R5\RollbackPreparation`：备份软件122文件、135,111,719字节，以及现场配置和两个旧启动任务XML。未复制DataStore、未封装全盘项目；这只是在线预备资料，记录明确 RequiresPostStopRefresh=true，不能替代本轮停机后的最终数据状态。

现场仍为PID20472、启动ticks639244771897870950、RunId0550acacc5334f51a293da950bf3b3eb，项目10243-028。已通过受限交互任务CodexR5FieldUi只读抓取窗口树，确认“停止试验”入口存在，未点击。当前运行中检查点的三项安全标志为false，不能当成停机安全证明。正常停止后须重新读取本次session的closing回执和检查点，再决定下一步；不能把当前false改写为true。

已在本地准备绑定上述身份的关闭辅助脚本CloseCurrentUi-R5.ps1和同基链增量脚本Seal-Incremental-R5.ps1，仅通过语法检查，尚未执行。C盘约1.94GB，切换还需处理历史部署目录占用；未删除或移动任何历史DataStore。当前旧版继续运行，尚未切换或启用自动恢复。

### R5停止预检后恢复旧版（2026-09-09北京时间）

本轮在旧版停止后的安装预检阶段结束。01:45:23通过正常界面停止，01:45:30收到事务 `35c33ff839c04a139ebfb78d6f5f7f83` 的完整回执：FinalSafetyResultCommitted、MotorsOff、PowerOff、PressureSafe、PersistenceDrained、LogicalQuiescent、DataContinuityVerified均true。但schema6旧检查点仍将MotorOffConfirmed、PressureSafeConfirmed、PersistenceDrained保存为false。当前安装器只认这些检查点标志或人工物理隔离确认，尚不能使用本次真实关闭回执；未修改标志，也未使用历史人工确认替代本次事实。因此未安装新程序，恢复旧版续测。

01:47在原窗口尝试开始未恢复，原监督会话已终结。随后依完整回执正常关闭旧进程，通过原MTTFTestAutoStart任务重新打开2.14.2.11。菜单展开和点击需在同一隐藏交互脚本内完成，避免脚本窗口切换使菜单收起。01:52:56重新开始；01:54:07六通道处于Learning；01:56:59和01:57:38两次相隔38.84秒快照确认六通道Running，逐通道正式圈和SQLite receipt_count均增加，ReadErrors为空。

当前主程序PID21176、启动ticks639244865537307857，Session `a4879c5c93af4320955959128daed704`，Run `de4ddc0445d4461e8e5d929d3adea4bb`。重新开始时剩余目标151663/151680/151777/151733/151742/151760，与停止前完全一致，没有回退数据库、DataStore或圈数。仅临时暂停的巡检已恢复并绑定新身份，临时UI任务已删除。

新增增量包 `D:\EPB_Data\Backups\10243-028_V2.14.2.11_I0003_0909_0141.7z`，15,978,191字节、97文件；01:42封存并7z t通过，属于原base id `abb1703af7f04b029b4ad21c9d85a0f8` 的序号3。已回传本机同路径，Robocopy退出码1、大小一致。它是停止前的在线增量，不冒充停止后最终封盘，不包含Current\DataStore。

证据：`artifacts/field-deploy-20260908-v3-round4/wj-r5-preflight/` 保存本次请求、关闭回执、旧检查点、会话记录和增量链；`legacy-resume-running-first.json`、`legacy-resume-running-second.json`、`legacy-resume-verified.json`保存正式恢复对账。首次读取无BOM旧检查点时因PowerShell默认编码解析失败，明确UTF8重读成功；不能把这次读法错误称为现场文件损坏。

后续先修复安装迁移：接受可核验且与本次Run/Session/旧进程身份绑定的完整关闭证明，拒绝旧试验、过期证明、证明后重新运行、部分失败与不一致状态；不改写旧检查点安全标志，不削弱无证据时的门禁。先用本次证据脱敏夹具和JXCQ验证，再重新生成一致版本包。旧版保持运行，自动恢复最终目标仍未完成。

### 停止回执迁移修复与验证

修复提交 `29fc9002a535c767c3daa31a34d6a842c4b752f4` 已进入独立发布工作区。安装器在已持有维护互斥、停止旧监督服务和启动任务后，对于缺少旧检查点安全标志的 schema5/6 会话，读取当前项目中唯一匹配 RunId 的会话和 closing 回执。核验实际旧 EXE 摘要、会话和运行代次、PID/启动时间、人工停止终态、七项完整安全结果以及不可重新拉起状态；通过系统进程查询单独证明主程序不存在。关闭回执中的 OldProcessExitProven=false 不被冒充为进程退出证明。

时效从 LastVerifiedActiveRun.CapturedUtc 计算，最多10分钟；会话一般 UpdatedUtc 的后续更新不能续期。检查点更新时间须处于最后有效活动观察与终态回执之间，迁移前重读检查点并再次检查主程序。证据随检查点归档，仅迁移剩余圈数，不迁移授权、不声称物理隔离、不改写旧安全标志。这10分钟是本次安装迁移的证据窗口，不改变独立恢复方案的60分钟断电过期策略。

Windows PowerShell5.1本地与JXCQ验证：23/23纯证据校验；7/7模拟实时文件读取和实际安装迁移函数测试。覆盖完整回执、七项部分失败、字符串假布尔、运行/暂停、旧Run、PID复用、不同映像/会话、未退出、过期/未来/时间反序、许可仍存活、非人工停止、检查点变化、主程序重现和会话更新时间不能续期。测试使用历史记录转换的隔离夹具，进程查询模拟；不属于现场物理安全验收。

JXCQ证据：`artifacts/field-deploy-20260908-v3-round4/jxcq-legacy-migration-tests.log` 与 `jxcq-legacy-migration-integration.log`。02:08现场只读快照仍为2.14.2.11/PID21176，六通道Running、正式计数和SQLite提交继续增长、ReadErrors为空。新提交完整包正在构建，尚未替换现场，旧9f44a76包不能冒充包含此修复。

自动恢复最终验收仍缺真实物理面板停机证据。面板按钮只能人工按下，已询问现场可配合的时间；远程操作和软件模拟不能代替这项事实。此项不影响继续构建、JXCQ无硬件验证和准备回退，但不能据此填写全部验收通过。
