# Guard 自动恢复候选安装包 0b59918

本次完成自动恢复安装入口补齐及完整包构建。默认 RecoverExited，可显式选择 RecoverStalled。不是现场自动恢复验收完成声明，wj-epb 本次未部署、未停止试验。

## 发布身份

- 版本：3.0.0.0。
- 安装工具提交：0b5991820a40；基础 Main/Guard 组件保持已验证的 94a5439b0d8f，未重编译运行时。
- 文件：`D:\Github\wanxiang\EPBTest\artifacts\deploy\V3.0.0.0_0b5991820a40_AUTO_RECOVERY_PENDING_ACCEPTANCE.7z`。
- SHA256：`A010E47A50AD3D6946779EC50EC2C4005345F9572865677E6852D1A666C7D60F`。
- 大小：34,333,324 字节；155 文件；7-Zip 最大压缩参数 mx=9，归档完整性检查通过。

## 本次修正

此前候选安装器固定 RecoverExited，范围小于方案中的有安全证明时恢复停滞。本次让 RecoveryMode 在验收、提权、已验收 Guard 材料生成、实际安装和结果输出之间一致传递，清单显式声明两种支持模式。

RecoverStalled 必须在真实报告 approvedModes 中获准且额外通过 stalledRecovery 场景；仅有 RecoverExited 报告会拒绝。根目录八个中文入口保留，双击安装和修复默认 RecoverExited；停滞恢复安装/修复需显式使用 PowerShell 参数 `-RecoveryMode RecoverStalled`。完整步骤见[使用说明](../RecoveryGuard_自动恢复候选包使用说明.md)。

自动恢复仍依赖新鲜运行授权、人工停止优先、60 分钟过期、持久化和数据对账、接管互斥与安全证明。只有人工停机按钮不等于具有独立软件断能反馈，无证明时不强杀。无 CAN 检测；I0050 修复仍为第二步。

## 验证边界

本地候选包检查 6/6：包完整性、无报告拒绝、错误机器拒绝、退出报告不能授权停滞，以及两种模式生成材料后通过真实 Guard 只读验证。后两项使用明确标记的合成夹具，未安装任务，也不构成现场验收报告。

证据：`artifacts/field-deploy-20260908-v3-round4/test-automatic-0b59918.log`。

基础组件此前的 JXCQ 八入口安装、监督启动、停止、卸载及回退记录沿用 94a5439 交付文档；不能宣称本次新包已再次完成整套生命周期实测。

## 现场限制和回退

未通过真实机器和台架绑定的验收报告时安装明确拒绝，不静默安装观察任务。现场 Guard 自动执行尚未启用。停止当前试验、覆盖部署或编造验收均不属于本次构建验证动作。

回退须正常停止并确认本次断能卸压及保存收口，增量封存，保留最新 Current\DataStore 和现场配置，再恢复匹配的旧程序、服务和任务。不能用旧数据覆盖最新试验进度。

## JXCQ 同包验证结果

同一归档 SHA256 一致，解压至 `C:\RecoveryGuard-3.0-Validation\自动恢复 候选-0b59918\解压 安装包`，中文及空格路径下 6/6 检查通过。归档保留为该上级目录的 `package.7z`。证据：`artifacts/field-deploy-20260908-v3-round4/jxcq-automatic-0b59918.log`。本次只读验证及 TEMP 夹具生成，无 Main 启停或任务安装。

## 后续审查：修复入口保留模式（源码已修，尚未重新入包）

发现 0b59918 包的 Repair 默认 RecoverExited 会使已装 RecoverStalled 降级。源码新增 Resolve-AutomaticRepairMode：未显式指定时读取已安装设置并保留自动模式；未知或缺失 Mode 拒绝覆盖。显式选择仍须经过报告批准检查。Test-AutomaticRepairMode.ps1 本地 7/7 通过，仅使用 TEMP JSON，没有修改安装状态。尚须重新构建并在 JXCQ 验证新包；本节不改变上面的 0b59918 归档身份和已验证范围。

恢复后台服务链路经核查调用 Stop-RelatedProcesses.ps1 Restore，按清场记录恢复任务 Enabled 状态，不写 guard-settings.json；这仅是代码核查，不冒充新包现场实测。
