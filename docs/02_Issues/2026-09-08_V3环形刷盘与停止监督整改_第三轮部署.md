# V3 环形刷盘与停止监督整改、第三轮部署记录

状态：第三轮现场验收失败。尽管正式构建、JXCQ 同包验证、现场安装及监督启动通过，现场学习后段仍发生积压及恢复阻断。已按用户最新要求优先回退，23:13 恢复 2.14.2.11 软件，旧版续测验证进行中；之后只做增量封存回传。后续修复、重新生成包并部署的目标仍未完成。

## 已证实的原因

第二轮现场包提交 78f74a6 未能完成学习和启动，已正常停止并回退 2.14.2.11。完整时间历程见[第二轮记录](2026-09-08_wj-epb_V3修复包_第二轮部署与观察.md)。

2026-09-08 21:46:06.824（北京时间）Dev2 写入耗时 1061.468 ms，其中环形文件刷盘 1020.831 ms、RawJournal 提交 2.348 ms、索引提交 1.527 ms。环形同步刷盘阻塞实时写入，触发约一秒的积压保护。第二轮回传已于学习前完成，不能把这次失败归因于回传并行。

旧版 2.14.2.11 没有新版 RawJournal 的周期性同步环形刷盘路径。本次参照旧版实时路径，将额外环形刷盘移出实时线程；保留新版 FULL 耐久日志，不降低积压保护阈值、不关闭数据连续性检查。

随后，停止逻辑清场已完成，但 StopSafetyTransactionRunner 的终态投影仍发布硬接管。sidecar 于 21:46:15.391 请求接管，使 21:46:15.067 刚开始的新批次被再次取消。前轮只更新调用方结果，未同步监督发布源，因此不足以修复此链路。

## 本次实现

- 每通道最多一个后台环形刷盘任务，使用独立、按已写入范围合并的映射视图，不占用实时状态锁，不在后台写 SQLite。
- 实时封圈仍应用所有已暂存帧。FULL 原始日志保留未确认刷盘的数据；观察到后台刷盘成功后，才提交对应索引前缀并回收该前缀日志。后台失败向调用方报告且保留日志。
- 正常释放映射前等待后台任务结束；重启时同步重放并确认耐久前缀。环形刷盘未完成的圈索引禁止被历史清理删除。
- 物理安全及保存已完整确认、仅逻辑尾声未结束时，停止监督保留活动状态及原截止预算，不提前发布不可撤销硬接管。
- 只允许同一事务、同一代次、未超阶段无进展预算及总截止时间的清场结果同步收敛监督源；旧事务、真实超时、数据缺口和硬接管不能清除。

发布源码提交：`b233939`。版本仍为 3.0.0.0，以完整提交号和包摘要区分修复包；旧包不覆盖。

## 已完成验证

- 本机存储回归 73/73：包含慢刷盘期间后续圈写入/封圈不阻塞、未成功刷盘不清理日志及圈索引、刷盘失败可重放、历史数据格式及导出检查。
- 电源测试 19/19。
- 干净源码正式构建：控制/恢复 803/803、存储 73/73、电源 19/19、现场数据门禁验证器 49/49、部署契约 41/41；简化部署契约 1/1、入口解析 5/5。
- 发布版 Guard 93/93，包含连续 300 次失败及冷却重试；本机无硬件测试，不代表现场 Guard 自动恢复验收。
- 最终包 JXCQ 验证与现场验证结果待下文补记。

损坏环形数据的测试增加了“先完成耐久日志回收”的前置步骤，否则日志正确修复数据会使原损坏断言失效；导出拒绝损坏的断言仍保留。

## 现场执行要求

遵守既有远程部署章程：正常停止、确认本次停止安全与保存证据，封盘脚本完全退出后切换 Current，回传可与安装并行，正式学习前等待回传结束。原项目计数和原始数据保留，不回放旧运行许可。正常关闭失败时依据本次可信证明处理，不能伪造安全字段。

2026-09-08 22:20:12 只读检查：旧版 PID 1920，六通道 Running，SQLite 机械提交与通道机械计数相符，ReadErrors 为空。另观察到指向历史 PID 22832 的 sidecar 短暂启动；本轮维护需核对历史监督残留并归档，不能影响当前主进程身份。

Guard 保持 ObserveOnly、Execution Disabled。无 CAN 硬件，不进行 CAN 检查。I0050 液压恢复状态发布一致性的独立修复仍列第二步。

## 最终包与实测结果

完整包：`D:\Github\wanxiang\EPBTest\artifacts\deploy\V3.0.0.0_正式版_b233939d014b_QUICKDEPLOY_GUARD.7z`。

SHA256：`983BE6574749B87429607A865378B6DF095CB1E2B0424B1366BEBF41D9C2C0A5`。大小 34,329,934 字节，140 文件，7-Zip 最大压缩率 `-mx=9`；包内 Main/Guard 同源，独立摘要和压缩完整性检查通过。

正式源码提交：`b233939d014b3372b0159b7fa3184d895cc9b5cd`，gitDirty=False。

本机实测证据：`artifacts/field-deploy-20260908-v3-round2/build-release-async.log` 与 `artifacts/field-deploy-20260908-v3-round3/`。JXCQ 同包验证及第三轮现场执行结果待补记；此处尚不宣称部署成功或问题彻底消除。

### JXCQ 同包验证

22:33:14 安装、22:33:17 状态检查、22:33:34 修复、22:33:38 恢复后台服务、22:33:41 故障采证均 ExitCode=0。22:33:59 交互桌面通过监督入口启动 PID 8444，窗口显示 V3.0.0.0，正常 CloseMainWindow 后退出。22:34:55 已完成停止全部、卸载及回退至 2.17.3.0；回退主程序 SHA256 与安装前相同，全部既有配置哈希相同，Supervisor Running、MainCount=0、GuardTasks=0。

测试路径：`C:\RecoveryGuard-3.0-Validation\刷盘修复 验证-b233939\正式 安装包\V3.0.0.0_正式版_b233939d014b_QUICKDEPLOY_GUARD`；压缩包为上级 `package.7z`，摘要与最终本机包一致。八个 CMD 入口实际执行，维护操作由管理员 WinRM 调用、启动与关闭由交互桌面任务执行，未模拟人工 UAC 点击。证据已回传 `artifacts/field-deploy-20260908-v3-round3/jxcq/`。

### 第三轮现场时间历程（北京时间）

- 22:38:26 停机前六通道 Running，无读取错误，完整计数在 `artifacts/field-deploy-20260908-v3-round3/baseline/`。
- 22:38:44 正常点击停止试验；22:38:50 完整停止回执通过，事务 `a34796451f864fab97d026f5bde21145`，会话 `f5c721666f1146108279124f1202e5ce`、Run `a319c8617adb4c068ddb6d7831070539`。电机关闭、电源关闭、压力安全、保存完成、逻辑静止、数据连续性均 true。
- 正常关闭辅助脚本首次未找到唯一窗口，未执行关闭；重新枚举确认同一 PID、启动时间及窗口句柄后，通过 WM_CLOSE 和 CloseMainWindow 正常关闭。发现错误的原始输出保留为 `close-discovery-error.txt`。
- 新版停止维护脚本发现历史 Guard 注册信息但共享状态不存在，安全拒绝继续。现场核实 Guard 任务、Guard 进程均不存在，主程序已正常退出，确认是第二轮卸载留下的注册文件；将精确 `C:\ProgramData\MTTFTestRecoveryGuard` 目录归档到本轮 `GuardRegistration-orphan-before` 后，维护停止成功。未伪造 Guard 或安全状态。
- 旧程序、ProgramData、LocalControl 分别封存为本轮 ZIP 并完整性校验，摘要保存 `backup-hashes.jsonl`。Supervisor 已停止时，将历史 `.launch.json` 登记移入 `SupervisorLaunches-before`，防止已退出 PID 对应的旧 sidecar 被重复拉起。
- 本轮远程工作目录 `D:\debug\CodexDeploy_20260908_V3_R3`。
- 22:50:11 封盘结束，脚本自带完整性检查通过：`D:\EPB_Data\Backups\10243-028_V2.14.2.11_B0001_0908_2242.7z`，130,102,599 字节，原始数据 2,346,708,577 字节。封盘 PowerShell 退出后才切换 Current；回传与安装并行，不追加封盘包解压或摘要复核。
- 项目旧 `log` 目录归档到本轮 `ProjectLog-before`，没有删除原始数据。
- 22:51:22 Main 和 Guard 安装成功，Guard ObserveOnly、Execution Disabled。已逐项对比主程序、Controller、DataOperation、Watchdog、SessionAgent 与最终包哈希一致；全部既有 ProgramData 配置哈希未变。
- 22:52:10 Supervisor 成功启动 Main PID 3884，窗口显示 `MT EPB常温疲劳测试 V3.0.0.0 [PID 3884]`。此时数据仍在回传，未开始学习和正式试验。
- 用户随后明确要求“直接开始，不用等回传结束”，覆盖此前等待回传的顺序要求。实时监视辅助脚本首次受未初始化 `$LASTEXITCODE` 判定影响，仅展开菜单；修正辅助脚本后正常打开实时监视，没有修改主程序或硬件准入。
- 22:56:58 通过正常“开始试验”入口启动；22:57:04 看门狗就绪，准备六通道自学习 10 圈。新 Session `ff61bfd45bc745bc839ed33d595928f3`，Run `5c140f4060ea4dab934b14c00bcee8c0`。
- 22:57:09 数据包回传完成，Robocopy ExitCode=1 表示成功复制新文件。本机路径 `D:\EPB_Data\Backups\10243-028_V2.14.2.11_B0001_0908_2242.7z`。实际回传与启动阶段重叠，不能将后续全部学习描述为在回传负载下完成。
- 22:57:15 六通道进入 Learning；22:57:53 已推进至第 2～3 学习圈，学习圈封存成功，未重现 DaqPersistenceLag 或接管取消。Supervisor、SessionAgent、sidecar 健康管道均 healthy；两设备队列未丢弃、无永久缺口，截取时积压约 2.6～2.8 ms。出现早期软峰值预警和启动期电源遥测约 820 ms，均未触发永久故障或停止，继续跟踪。
- 22:58:51 仍在推进第 6 圈学习。随后 Dev2 两次触发积压，incident 时间分别为 22:59:07.576、22:59:48.732；恢复期间 RunId 发生改变，部分通道暂停，23:00:02 记录 TakeoverExitBlockedWithoutApprovedPermit。不能把本轮写成首次学习或正式试验通过。
- 第二份 incident 捕获 22:59:38.805 写入 941.374 ms，但 RawCommit 1.495 ms、RingFlush 0.011 ms、Checkpoint 11.055 ms。大部分耗时落在现有细分指标没有覆盖的位置；这次不能继续直接归因为原同步 RingFlush。第一份 incident 没有捕获完整在途长写入，需结合代码、锁等待和后续诊断继续定位。
- 23:00:38 正常按钮停止新版，23:00:46 停止事务 `897b26eb60264e1d8f8257d299dd34b9` 的物理安全、保存、逻辑及连续性均 true。窗口随后显示“自动替换许可未形成，已阻止旧进程退出”，正常 WM_CLOSE 等待 60 秒仍不退出。保存正常关闭失败记录及转储尝试结果后，凭本次完整停止证据、精确 PID/启动时间及用户既有残留处理授权，经受支持维护脚本停止相关进程。
- 回退保留新版 Current、ProgramData、LocalControl 和 Guard 注册状态至本轮独立目录，并保留项目数据库副本。检查原始日志待应用帧为 0、机械总回执 295050 未改变，不存在待重放帧，因此未执行日志重放写入。
- 恢复旧版软件时，将新版最后的 `DataStore` 原样复制回运行目录；没有用旧备份覆盖最新环形采样，没有回退项目数据库或圈数。旧版 EXE SHA256 `9158A6CF57CFD6329D3F8DC3DD254F5E35A0D8C8B9FE48CD43B9A3919837A8BA` 与停机前一致。
- 23:13:09 旧版 PID 20472 启动，已恢复原最高权限任务并清理被卸载 Guard 注册残留。部署脚本首次被远程执行策略拒绝后，采用独立 PowerShell 进程级 Bypass 重试成功，未修改系统永久策略。旧版正式续测及增量回传结果待补记。

## 后续封存规则（用户最新要求）

后续只在当前完整基包链上生成增量，不再默认传 `-StartNewBase`。先恢复旧版运行，再增量封存、回传；本轮已经完成的完整基包保留，不重复封装。若既有链确实不可用，应先核实原因，不默默退化为又一次全量封装。

### 回退续测与增量封存结果

- 23:13:09 恢复 2.14.2.11，PID 20472；23:18:07、23:18:44 已确认六通道全部 Running，正式计数与 SQLite 保存持续推进。
- 23:24:15 回传期间复核：同一主进程运行，六通道 Running，读取错误为空；相对 23:18:44，每通道机械计数增加 22 圈，SQLite 最新提交时间为 23:24:09～23:24:11。此结果证明当时旧版已恢复续测，不代表无限期稳定性保证。
- 23:20:44 增量封存完成，沿用基链 `abb1703af7f04b029b4ad21c9d85a0f8`，序号 2，没有新建全量基包。文件：`D:\EPB_Data\Backups\10243-028_V2.14.2.11_I0002_0908_2319.7z`，21,545,771 字节，402 个文件；脚本自带 7-Zip 完整性检查通过。
- 23:23:36 开始仅回传上述增量包至本机同名路径，原完整基包已在本机，无需重复传输。回传结果见 `artifacts/field-deploy-20260908-v3-round3/return-incremental.log`。
- 已移除本轮临时交互任务 `CodexFieldV3Round3UI`；正式自动启动和会话任务保留。已启用绑定本次旧版进程、Session、Run 的 24 小时只读观察，每 30 分钟检查，有意义异常通知。
- 新版第三轮失败包仍不得标记稳定通过；后续在旧版继续试验期间分析新版本剩余阻塞及人工关闭问题。

增量回传最终结果：Robocopy ExitCode=1（成功复制），本机目标文件已落地；未重复封装或回传全量基包。

### 第四轮本地定位准备（尚未形成修复包）

已补充生产写入诊断：`RawAppendMs` 覆盖原始日志序列化、摘要、查重与追加；`RawAppendGateWaitMs` 单独记录追加锁等待；`ViewRemapMs` 覆盖映射创建、交换与旧视图释放；`RingWriteMs` 记录实际 WriteArray 调用。上述阶段可嵌套，不能相加反推未覆盖耗时。采集 CSV 原先将 RingFlushMs 投影为 RingWriteMs，现改为实际映射写入耗时，刷盘耗时继续保留在 Detail 中。

磁盘回归 73/73 通过，日志 `artifacts/field-deploy-20260908-v3-round3/test-diagnostic-coverage.log`。学习批次新增真实写入诊断断言；已有诊断回调抛错不改变耐久结果的测试通过。没有降低积压阈值或改变保存策略。本次修改尚未发布、尚未得到现场慢写入的新阶段证据，不能把诊断补齐写成根因修复完成。

关闭路径复核发现 `BeginWatchdogClose` 对任何非空 `_watchdogCloseTask` 都复用，包括已经返回失败的任务；同时退出意图是可被后续请求改写的共享字段。需补充“自动接管失败后人工正常关闭”的可重复回归和精确意图边界，再决定修复，不能仅凭历史窗口标题删除许可检查。原第三轮完整转储和日志保留，现场旧版继续运行。

第四轮退出重试修改：主窗体仅加入仍在执行的关闭任务；已结束但未取得退出许可的尝试允许重新执行安全准备及授权。原退出期限、身份许可与窗口释放条件保留。新增生产协调入口回归，覆盖并发关闭不重复准备、拒绝授权不释放、失败后人工重试重新授权、最终授权仅释放一次。新增用例尚待下一次编译执行，当前进行中的全量控制回归是新增用例加入前构建，不能作为此用例通过证据。

用户明确允许 JXCQ 测试。已只读确认 JXCQ 当前安装 2.17.3.0、主程序未运行、C 盘约 58.7 GiB 可用；前次测试与回退证据仍存在。下一候选包在独立中文含空格目录验证，不占用 wj-epb 正式运行。

### 第四轮已完成验证

- 本机控制全量回归：803/803，`test-close-retry.log`。此构建早于新增协调器用例，不能写成 804/804。
- 随后重新编译最终协调器修改，UI 专项 30/30，`test-close-retry-final.log`，明确包含 `FailedCloseAttemptCanRetryAuthorization`。该用例覆盖真实协调器入口，但不等同于第三轮真实硬件恢复失败与完整主窗体退出的端到端重现。
- 电源测试 19/19，`test-power-diagnostics.log`。
- JXCQ 隔离磁盘回归 73/73，证据回传 `artifacts/field-deploy-20260908-v3-round3/jxcq-disk-diagnostics.log`。远程测试目录 `C:\RecoveryGuard-3.0-Validation\第四轮 诊断测试\disk`，含中文和空格；使用本机已编译的磁盘测试依赖包，不改动 JXCQ 已安装的 2.17.3.0。此测试不是完整安装包部署验收。

上述测试均已结束，退出码为 0。当前工作区新增计时和关闭任务重试修改尚未同步至干净发布树，尚未生成新最终包。下一步需进一步定位实际慢写入、验证人工意图与自动恢复竞争，以及最终候选包的 JXCQ 安装生命周期。wj-epb 继续运行旧版，本轮未进行现场切换。

### JXCQ 双设备学习写入探测

新增 `EpbDiskWriterTests.exe --learning-timing-probe`：隔离临时目录、两个并发设备、六通道、每 10 ms 批次 20 个样本，各设备六轮学习封圈（每轮每通道 10,000 样本），合计 6,000 设备批次、36 次通道封圈。每次封圈前检查样本计数，不调用任何硬件，不读取现场项目。

首轮使用 NewPolicy 默认 1 MB 文件，JXCQ ExitCode=0，最大设备批次 41.535 ms，未捕获 >=50 ms 的批次；证据 `artifacts/field-deploy-20260908-v3-round3/jxcq-learning-probe.log`。随后将探测专用文件大小设为现场 100 MB 重测。此模拟没有复现现场既有数据库体量、磁盘剩余空间、UI、NI 和停止恢复并发，不得将未复现写成现场故障已解决。

100 MB 复测已完成，ExitCode=0，6,000 批次全部完成，最大 61.554 ms。两个相邻设备批次出现竞争：Probe0/986 总计 61.554 ms、RawStage 55.839 ms；Probe1/985 总计 60.851 ms，其中 RawAppend 58.996 ms、AppendGate 58.384 ms，实际映射写入仅 0.004 ms。证据 `artifacts/field-deploy-20260908-v3-round3/jxcq-learning-probe-100mb.log`。

这证明设备事务已提交后，每通道再次进入 AppendRawJournal 查重仍会等待另一设备的原始日志锁，造成额外批次延迟；尚未证明它是现场 941 ms 的全部原因。下一步可针对同一次 WriteDeviceBatch 已完整提交的精确帧避免重复数据库访问，必须保留摘要冲突拒绝、失败事务不可发布、后续独立写入重新持久化等约束，并增加竞争回归后再评估候选包。

### 已提交设备帧复用修复与验证

每个通道在同一设备批次内持有一次性已提交凭据，包含周期、起始样本、数量、环形位置、代次及 SHA256。凭据只在 FULL 事务成功提交后发布，后续映射写入重新计算摘要并核对身份后消费，避免再次等待另一设备的原始日志锁；批次入口及 finally 均清理，不能跨批复用。事务异常不发布凭据。

新增测试通过提交后修改输入，确认抛出身份冲突、样本计数不推进；恢复原数据后精确重试、封圈与导出成功。首次回归发现直接注入阶段数据后凭据残留，已补批次入口清理并重跑，最终磁盘 74/74。证据 `test-frame-reuse.log`。

JXCQ 100 MB 同配置复测 6,000 批次通过，最大 58.219 ms；慢批次 RawStage=58.080 ms、RawAppend=0.459 ms、AppendGate=0.001 ms，实际写入 0.003 ms。证据 `jxcq-learning-probe-frame-reuse.log`。前次最大 61.554 ms 不是严格性能基准，不能仅凭峰值差宣称整体提速比例；本次证据支持重复追加锁等待已避开，仍保留必要的 FULL 事务提交。

最新完整控制回归正在执行，日志 `test-frame-reuse-controller.log`（本地 exec 53106）。当前尚未构建新的最终发布包，现场 941 ms 仍需完整验证；不改动 wj-epb 旧版试验。

### 发布源码同步与候选构建

本轮 9 文件已同步至 `artifacts/ReleaseValidation-20260908/source`，提交 `9690888`（`fix(storage): 复用已提交批次凭据并允许安全退出重试`），提交后工作区干净。主工作区其他修改未纳入该提交。

已启动正式 Build-Release，统一版本仍为已迭代的大版本 3.0.0.0，本次用提交身份区分候选。输出根 `artifacts/releases/V3.0.0.0-9690888-candidate`，日志 `artifacts/field-deploy-20260908-v3-round3/build-release-9690888.log`，本地 exec 51898。构建、同源 Guard 包、完整 7z 包及 JXCQ 最终包验收尚未完成，不将候选标记现场稳定通过。

本轮 JXCQ 安装生命周期脚本已准备于 `artifacts/field-deploy-20260908-v3-round4/Test-FinalPackage-Jxcq.ps1`，固定使用独立 `批次修复 验证-9690888` 目录及本提交包名，语法解析通过。脚本保留安装前配置与程序备份，明确将交互启动/关闭异常作为失败返回，回退配置采用子进程级 ExecutionPolicy Bypass，避免既有执行策略导致回退命令未执行。脚本尚未对新包执行，不能计作验收通过。

9690888 正式构建阶段结果：Release 控制 803/803、磁盘 74/74、电源 19/19 均通过。构建当前进入 Tools.test_validate_epb_field_gate 验证器测试（实际 Python 进程仍在运行，exec 51898 未结束）。这属于验证器自身测试，不是 wj-epb 硬件验收；Main 发布目录和完整包尚未确认生成。最新主工作区控制回归也已结束，803/803。

9690888 Main 发布已完成，目录 artifacts/releases/V3.0.0.0-9690888-candidate/V3.0.0.0-969088866698-20260908_155633，部署契约 41/41 通过。同源 Guard 随后构建成功，但完整包 Verify-FieldPackage 明确拒绝共享 RecoveryControl DLL 哈希不一致：Main=CA24E7FC3359CDC5C15B8737123B9B8EB47D02496CAC4F2225048BED61ED8A7D，独立 Guard=68F1502CC4AA541DB8623FDB9C20CD4AAF0E5D7FD41063BB3925E2EA95C08C8E。仅改用 Any CPU 重新构建共享项目得到另一哈希，不能据此归因为单一平台参数。保留未完成目录供调查，没有生成有效 7z 或部署到 JXCQ。下一步需统一共享组件构建输入与发货来源，保留校验门禁。

2026-09-09 共享组件问题进一步定位：两份 RecoveryControl 经 ILDasm 对比，程序集版本和托管代码一致，差异为 MVID、工具显示的 Image base 及反汇编资源输出路径；不能把此结果当作字节一致证明。已在 Guard 打包工具增加 MainReleaseDirectory，完整校验同版本、当前干净提交的 Main 包后复用其共享 DLL/PDB，保留最终哈希检查，不改写既有包清单。不同提交 Main 拒绝测试通过。变更提交 8c24d7e，已启动按新身份的完整发布构建，日志 artifacts/field-deploy-20260908-v3-round4/build-release-8c24d7e.log。新包未完成前，9690888 不作为完整交付物。

2026-09-09 00:03:13 只读复核 wj-epb：2.14.2.11 六通道 Running、读取错误为空，正式计数 47195/47174/47088/47121/47112/47091，证据 artifacts/field-deploy-20260908-v3-round4/legacy-status.json。新提交8c24d7e已编译完成、发布回归正在执行（exec 95764）。JXCQ脚本已更新绑定8c24d7e9ef76；后续合包入口 Complete-Bundle.ps1 已准备，尚未运行。

### 8c24d7e 完整候选包已生成

2026-09-09 发布构建及合包结束，完整包 `D:\Github\wanxiang\EPBTest\artifacts\deploy\V3.0.0.0_正式版_8c24d7e9ef76_QUICKDEPLOY_GUARD.7z`，34,327,483 字节，140 文件，SHA256 `7F0BDDA94FE2F6D99A84DF33ABDB79DA7912DD158682B0F88150AC99402F8540`。最大压缩率打包及 7-Zip t 完整性检查通过；Main 与 Guard 共享组件一致性校验通过。Guard ObserveOnly、Execution Disabled。

正式构建控制803/803、磁盘74/74、电源19/19、门禁验证器49/49、部署契约41/41通过。此包尚未完成 JXCQ 安装生命周期验收，不能标记现场问题已彻底解决。已启动传输及远程安装验证（exec 51557），目标目录 `C:\RecoveryGuard-3.0-Validation\批次修复 验证-8c24d7e`，先比对本地SHA256，再解压安装。现场旧版未切换。

JXCQ 首次执行在辅助测试脚本解析阶段失败（PowerShell 7 写出的 UTF-8 无 BOM 被远程 Windows PowerShell 5.1 按本地编码解释），未进入安装。已仅将辅助脚本改为 UTF-8 BOM 并回传重试（exec 5465）；正式安装包及 SHA256 未改变。

第四轮完整包JXCQ安装、监督启动、正常关闭、维护、卸载及回退已通过，最终组件Guard93/93通过。完整交付记录转至 [2026-09-09_V3完整包交付与JXCQ验证.md](2026-09-09_V3完整包交付与JXCQ验证.md)。wj-epb本轮未切换，现场问题仍需后续验证。
