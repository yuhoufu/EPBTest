# V2.12.0.16—V2.12.0.27 上线要求完成性追踪矩阵

## 结论

本矩阵逐项对照[全历程根因与彻底整改方案](./2026-08-08_10358-029_V2.12.0.0_全历程卡顿与多故障根因及彻底整改方案.md)第 8—12 节。当前结论是：**V2.12.0.26 完成了 V24 全停链的第一轮整改，但封版反证又发现伪持久化水位、恢复/Stop 先等所有权后断能、异步 OFF 永久等待、日志刷新竞态和现场门禁假阳性。V2.12.0.27 是修复这些第二轮问题后的当前候选。自动化只能证明代码结构和可控并发语义；真实六通道 2/8/24/72 小时、故障注入和每通道 100000 圈尚未完成，因此仍不能签字为生产无人值守上线版本。**

V2.12.0.24 现场证据与 V2.12.0.26 整改详情见
[V2.12.0.24 全停不可恢复根因与 V2.12.0.26 整改方案](./2026-08-10_10358-029_V2.12.0.24_全停不可恢复根因与V2.12.0.26整改方案.md)。

V2.12.0.27 第二轮反证、物理持久化/明确排除边界和最终验收命令见
[V2.12.0.27 长期无人值守第二轮整改与验收报告](./2026-08-10_10358-029_V2.12.0.27_长期无人值守第二轮整改与验收报告.md)。

本次完成性审计又补齐了一个发布证据缺口：正式 SESSION 现在记录程序集版本、PID、完整 EXE 路径、EXE/配置 SHA、Git SHA、dirty 状态和构建时间；主窗口标题显示版本与 PID；每个 DAQ 事故阶段及事故写入失败的回退目录都写入 `build-identity.json`。现场门禁会直接拒绝缺 PID/路径/程序集版本的正式会话，或缺完整构建身份的事故阶段。

## 第 8 节结构整改追踪

| 主文档要求 | 当前实现与直接证据 | 软件状态 | 现场状态 |
|---|---|---:|---:|
| 8.1 峰值语义、软预警、日志和 UI 放大链 | 峰值语义、WarningSnapshot 有界 gate、UI 512 条/50 行；普通日志 1 s 合并软刷，硬证据才 durable；写入/刷新使用版本水位，Shutdown 有准入屏障 | 主要行为已完成；真实日志/磁盘长尾待验 | 24 h 计数待验 |
| 8.2 原始数据与圈索引分层 | Dev1/Dev2 独立有界处理/持久化 worker、SQLite 圈终态、sequence/gap/所有权和显式排除账本已有直接测试；但仍是 ConcurrentQueue/池化批次与共享物理存储，不是主文档字面要求的预分配 SPSC ring + 独立 helper 进程 | 行为耐久已覆盖；指定最终拓扑未完成 | P99/P999、共享盘与 0 静默丢批待验 |
| 8.3 DO 实时安全隔离 | 自适应终态 OFF 的真实 subscriber 只做 <20 ms 有界提交；物理完成/电流清零后才推进；永久阻塞有独立 deadline；准入/Dispose 精确一次。方向/压力写仍未统一到完整预分配优先级环 | 终态 OFF 关键链完成；完整统一调度器部分完成 | 真实 NI P99/最大值及外部联锁待验 |
| 8.4 事故恢复与证据合并 | 根事故 correlation、先 TryAdd 后 epoch、终态证据异步提交、恢复前断能、冻结 sequence 边界和永久 gap 进程回收均有组件测试；100 症状测试主要验证内存 gate，不等于真实文件端到端 | 关键状态机完成；现场组合注入待证 | 受控设备故障注入待验 |
| 8.5 液压、Timer、活动圈统一生命周期 | 当前是分散式圈身份、幂等终态守卫、恢复所有权、run-scoped cohort/attempt 和 Task 监督；仓库没有字面 `CycleAttemptContext` 统一所有者，经理层端到端生命周期尚未完全证明 | 部分完成，不再宣称不存在的统一类型 | 真实压力、机械释放、退出排空及长期收敛待验 |
| 8.6 自适应控制修正 | 快速夹紧全速率证据、单圈限幅、污染模型隔离和重新学习已做；延迟模型当前主要使用 median/MAD，尚无完整 P95 与 decision/write/current-clear 四时间戳模型 | 主要控制修正完成；完整延迟模型部分完成 | 六通道 ≥99% 合格带且无 >17 A 待验 |

## 第 10 节必须新增测试追踪

| 编号 | 要求 | 自动证据 | 结果 |
|---:|---|---|---:|
| 1 | 有效捕获且 lag=25 ms 不报警 | `NonComparablePeakWindowIsNotProcessingLag` 及证据尾差测试 | 通过 |
| 2 | lag=101 ms 只报警一次 | classifier 与终态去重组件测试；缺少同一 manager 反复输入的完整事件链注入 | 部分通过 |
| 3 | 时间戳缺失使用独立诊断 | 峰值捕获质量分类测试 | 通过 |
| 4 | Dev2 DO 阻塞不拖慢 Dev1 | 真实 `FeedCurrentSample` subscriber 链模拟 NI 长阻塞，提交 <20 ms；另一设备不受拖累；100 ms 独立 deadline 硬故障收口 | 通过（模拟 NI） |
| 5 | 1000 次软预警仍有界并合并 | WarningSnapshot gate 并发/容量/间隔测试 | 通过 |
| 6 | 100 次事故只有一个上下文和终态 | `HundredIncidentSymptomsMergeIntoOneContext` 证明内存 gate；真实 trigger/terminal 文件组合仍待注入 | 部分通过 |
| 7 | 活动圈超过 37500 只作废一次 | writer 上限和 coordinator 原批重试已直接覆盖；manager 自动唯一终态主要为组件组合 | 部分通过 |
| 8 | DAQ 恢复时液压成员及时退出 | 恢复所有权、截止顺序、迟到旧代、run 隔离与 Stop 先断能策略测试 | 通过（组件），真实液压待验 |
| 9 | 快速真实上升立即 OFF 但不误硬报警 | `FullRateRapidClampBeforeLoadRiseIsAccepted`、全速率最终归因 | 通过 |
| 10 | 10 万条日志/UI 保持有界 | UI 10 万条直接压力；项目日志 10 万写入/20 万 flush 合并及精确 Flush/Shutdown 竞态测试 | 通过（本机合成） |

磁盘 50/100/500/1500 ms 暂停已经由 `DiskPauseMatrixRemainsBoundedAndRecovers` 自动覆盖；DAQ 空窗、DO 延迟、电源遥测、退出排空也有模拟测试。模拟测试不能替代真实 NI、程控电源、液压和主机资源竞争注入。

V2.12.0.17 补充退出反例：`StopSafetyExitPolicy` 明确验证持久化边界未确认时 `CanCloseApplication=false`，`ApplicationClosing/ProgramExit` 不得关闭 writer；边界确认后才允许结束进程。代码和现场注入方法见[V2.12.0.17 退出数据耐久门禁与流畅运行补充整改](./2026-08-09_V2.12.0.17_退出数据耐久门禁与流畅运行补充整改.md)。

## 第 11 节上线门槛证据状态

| 24 h 六通道门槛 | 软件是否记录/强制检查 | 当前是否已有合格现场证据 |
|---|---:|---:|
| 峰值证据语义 | 是 | 否 |
| DAQ 最大空窗及 QueueFull/ClockInvalid | 是 | 否 |
| 控制 subscriber P99 <5 ms、最大 <20 ms | 是 | 否 |
| OFF P99 <50 ms、最大 <100 ms、超限硬联锁 | 软件指标是；硬联锁须外部证明 | 否 |
| 持久化 Oldest/Depth/Pause/Failed | 是 | 否 |
| SQLite integrity 与 running=0 | 是 | 否 |
| 普通 warning/事故目录体积 | 部分：红线与身份强制；体积趋势仍需人工/现场核对 | 否 |
| 日志刷新及 Dropped=0 | 是 | 否 |
| UI 响应 P95 <200 ms | 是 | 否 |
| 单事故、epoch、重复启动、未观察异常 | 部分：终态/关联号强制；epoch 连续性和未观察异常缺完整端到端门 | 否 |
| 液压 stale pending 与 1 s 收敛 | 部分：超时红线；尚无完整 1 s 收敛计算 | 否 |
| 控流 ≥99%、无 >17 A、EPB8 无振荡 | 部分：合格率/峰值强制；EPB8 交替振荡需现场专项 | 否 |
| 序号对账、0 静默丢批、圈终态闭合 | 是 | 否 |
| 1 Hz 主机 CPU/磁盘/工作集/句柄/空间 | 部分：心跳和系统 CPU 强制；其余资源趋势待现场判定 | 否 |

任何一行“现场证据=否”都意味着不能把自动化测试外推为生产合格。正式验收必须使用同一连续 RunId、同一 EXE SHA 和同一配置 SHA；整批重建或进程重启后应从新 RunId 重新累计时长。

## 当前自动证据与版本状态

| 项目 | 结果 |
|---|---|
| 控制/恢复/DAQ/持久化综合测试 | `354/354`（含 V2.12.0.24 全停链、入口拒绝永久 gap、冻结物理耐久/有限排除边界、异步 OFF 硬截止、日志关闭竞态、禁止同进程伪恢复、进程回收退避及旧 Run 隔离反例） |
| SQLite/BIN/Raw 写盘测试 | `32/32`（含不同数据根目录并行映射、Raw 首次 Flush、257 批硬容量背压和写失败重试） |
| 现场门禁测试 | `38/38`（含72 h/每通道圈数、心跳覆盖、STOP独立边界、管线水位、排除账本和身份反例） |
| 电源调试器测试 | `17/17` |
| V2.12.0.27 六通道 2 kHz 持久化浸泡 | `600 s`、`PASS 1/1`；每通道 `1,200,000` 点，Dev1/Dev2 最大深度 `5/5`，Drain `0.0 ms`，SQLite checkpoint `1200` |
| V2.12.0.27 Release 交付门禁 | 必须从独立洁净工作树正式构建，0 error，且 `gitDirty=false`、`deploymentApproved=true`；实际身份以交付目录内文件为准 |
| ProductVersion | `V2.12.0.27` |
| 正式配置文件 | `7`，不存在 `TestConfig - 复制*.xml` |
| 隔离验证候选 SHA-256 | 不在文档硬编码；以交付目录的 `build-identity.json`、`SHA256SUMS.txt` 为唯一依据 |
| 递归身份/校验文件 | 必须由 `Verify-Release.ps1` 对交付目录实时复核 |
| 正常候选校验 | `Tools/Verify-Release.ps1 -ReleaseDirectory "<不可变候选目录>" -RequireDeploymentApproved`；封版后必须得到 `verified=true` |
| 多余备份配置反例 | 校验拒绝 |

V2.12.0.23 曾生成 `gitDirty=false` 候选，但完成性反证发现已接收批次仍有七类丢弃/伪推进路径，因此已作废。V2.12.0.24 随后完成耐久收口，但现场又证明同步诊断会阻塞处理队列，且可变 `bin\Release` 目录能被 VS 覆盖成同版本混包；V2.12.0.25 关闭发布混包路径，V2.12.0.26 完成 V24 全停链第一轮整改。第二轮反证又发现 V26 的抑制伪持久化、先等锁后断能、异步 OFF 和日志竞态，因此 V26 已被取代。V2.12.0.27 当前是待封版候选源码；只有从最终洁净提交生成独立不可变包并通过逐文件校验后，才成为现场验收候选。最终 Git SHA、BuildUtc、EXE/config SHA 和批准状态以交付候选目录内 `build-identity.json`、`SHA256SUMS.txt` 为唯一依据。

发布构建现已增加旧式项目关键源码门禁：`Controller` 的 `EpbManager.FieldMetrics.cs`、`LatestPairMailbox.cs`、`UiCurveContinuityPolicy.cs`、`TaskSupervisor.cs`，以及 `IO.NI` 的 `CoalescingTaskSupervisor.cs`、`HostRuntimeProbe.cs` 必须同时存在并列入 `<Compile Include>`；否则在 MSBuild 前直接拒绝生成，避免 VS 缓存或遗漏项目登记再次产出缺类候选。

V2.12.0.18 又补充了跨数据目录并行写盘反例：两个独立 `DataStorePath` 的写盘器可同时映射、写入和封口，同一数据目录仍使用相同稳定作用域保持互斥；`309/309` 控制综合回归与 `29/29` 写盘回归并行执行均通过。详见[V2.12.0.18 内存映射命名隔离与并行写盘闭环](./2026-08-09_V2.12.0.18_内存映射命名隔离与并行写盘闭环.md)。

V2.12.0.19 又移除了主程序对重复 `ZlgCanComm` UI 类型的无用依赖，将本地 `ToggleButton.cs` 纳入旧式项目清单，并让发布构建先清空经过绝对路径校验的专属输出目录。发布身份现在递归覆盖实际包的 97 个文件，SHA 清单覆盖 98 个文件；配置 SHA 只取项目明确发布的 XML，Windows PowerShell 5/PowerShell 7 计算一致。详见[V2.12.0.19 发布依赖去重与递归身份闭环](./2026-08-09_V2.12.0.19_发布依赖去重与递归身份闭环.md)。

V2.12.0.20 进一步确认“项目明确发布”仍不足以证明配置集合正确：两份历史 TestConfig 备份曾被项目错误标记为复制输出。现构建前、构建后和独立校验三处都强制只允许七份正式 XML；人工添加备份配置会直接校验失败。详见[V2.12.0.20 发布配置唯一性门禁](./2026-08-09_V2.12.0.20_发布配置唯一性门禁.md)。

V2.12.0.21 现场反证确认 DAQ `Discontinuities=0`、无容量丢批时仍可能出现视觉断线：UI 最大延迟超过旧绘图 300 ms 断线阈值，最新显示批覆盖被误当成采集断点。现断笔只看相邻 DAQ 批次时间，不增加 UI 队列、帧率或点数；新增连续/真实断点和十万次无分配测试。详见[V2.12.0.21 实时曲线显示断线根因与性能无损修复](./2026-08-09_V2.12.0.21_实时曲线显示断线根因与性能无损修复.md)。

V2.12.0.22 对同一现场会话继续量化：UI 最大消息泵停顿 5868 ms，37.9% 的统计窗口至少出现一次 >200 ms Flush；根因是第二套 `TextChanged` 处理对每批日志全量读取 Lines、重建 Text、裁剪和滚动。现改为唯一增量所有者、原位头删、复用批次与滚动限频，并将单次 Flush/Append/Trim/Scroll <200 ms 纳入现场门禁。详见[V2.12.0.22 UI 日志全量重写卡顿根因与彻底整改](./2026-08-09_V2.12.0.22_UI日志全量重写卡顿根因与彻底整改.md)。

V2.12.0.20 最终封存又证明 Dev1 最后一个序号已分配但未被后台队列接收，旧逻辑却永久等待其落盘；关闭处理每次重新执行 10 秒 StopAll 并弹框。V2.12.0.23 分离已分配/已接收序号，退出前冻结最终 DAQ 边界，同一关闭故障只提示一次。详见[V2.12.0.23 长期曲线退化、DAQ 末批边界与退出弹框根因整改](./2026-08-09_V2.12.0.23_长期曲线退化_DAQ末批边界与退出弹框根因整改.md)。

V2.12.0.24 继续反证并修复已接收批次在 generation 失效、Raw 队列满、SQLite 队列满、generation 清理、DaqAIContext 队列/首次 Flush/写失败以及 Published 伪推进中的丢弃路径；容量满改为后台有界背压，旧 generation 只退出实时链但必须完成归档。本地 6.27 GB V2.12.0.20 封存已执行 full/full/full 负控，结构化地拒绝会话未闭合、队列满、DAQ 空窗、UI 超时和自维护滞留。详见[V2.12.0.24 已接收批次全链路耐久与退出收口完成性整改](./2026-08-09_V2.12.0.24_已接收批次全链路耐久与退出收口完成性整改.md)。

V2.12.0.25 进一步确认 V2.12.0.24 正式候选曾与 VS 共用 `MTTfTest\bin\Release`：现场 EXE SHA 与已批准候选不一致，旧发布清单校验 `Config.dll` 失败。现暂存目录固定为不可部署，正式包原子生成到版本化独立目录；程序在新试验和跨进程恢复边界重新校验整包文件集合和 SHA。详见[V2.12.0.25 发布目录混包根因与不可变候选整改](./2026-08-09_V2.12.0.25_发布目录混包根因与不可变候选整改.md)。

正式现场门禁现由 `Tools/Invoke-Field-Acceptance.ps1` 统一执行：先对 Release 的递归身份、SHA 清单、七份正式配置和部署批准状态做独立校验，再把 ProductVersion、EXE SHA、配置 SHA、GitCommit、BuildUtc 与连续运行 SESSION 逐项绑定。V2.12.0.20 旧日志负控在 7.5 秒内被拒绝；`quick/none` 诊断扫描也被明确禁止用于正式放行，避免旧包、异包或不完整证据误判通过。

补充离线持续压力证据：早期 120/180/300/600 秒六通道 2 kHz 真实 SQLite/BIN 写盘均通过；V2.12.0.27 最终冻结工作树又独占复跑 600 秒，每通道 1,200,000 点，Dev1/Dev2 最大深度 5/5，Drain 0.0 ms，SQLite 进度事务 1,200 次，`PASS 1/1`。早期证据见[V2.12.0.17 六通道 2 kHz 离线持续写盘压力验证](./2026-08-09_V2.12.0.17_六通道2kHz离线持续写盘压力验证.md)，V2.12.0.20 结果见[发布配置唯一性门禁](./2026-08-09_V2.12.0.20_发布配置唯一性门禁.md)。这项证据将“持久化实现持续跟得上”提升为更强的离线验证，但现场 P99/P999、网络共享尾延迟和 24/72 小时资源仍为未完成。

## 剩余闭环顺序

1. 交付门禁：V2.12.0.27 必须在独立洁净工作树生成并校验为 `gitDirty=false`、`deploymentApproved=true` 的不可变候选；不得从 `bin\Release` 部署。
2. 待现场：2 小时受控冒烟及恢复故障注入，重点验证 processing 阻塞不再拖垮采集、永久 gap 不同进程伪恢复、进程回收按有界策略执行。
3. 待现场：8 小时资源与设备故障压力。
4. 待现场：24 小时六通道联合验收。
5. 待现场：72 小时无人值守稳定性。
6. 每阶段执行 `Tools/Invoke-Field-Acceptance.ps1 -MinimumHours <2|8|24|72> -MinimumFormalCyclesPerChannel <阶段值>`；阶段 PASS 仍固定 `production_release_approved=false`。最终生产放行只能使用 `-FinalProductionAcceptance -MinimumHours 72 -MinimumFormalCyclesPerChannel 100000`，并保留 MD、JSON、正式 Release、EXE/config SHA 和完整原始目录。

自维护专项设计与三次熔断证据见[V2.12.0.16 自维护有界熔断与整批自动重建整改](./2026-08-09_V2.12.0.16_自维护有界熔断与整批自动重建整改.md)；退出资源生命周期证据见[V2.12.0.16 后台任务排空与关闭顺序闭环](./2026-08-09_V2.12.0.16_后台任务排空与关闭顺序闭环.md)。
