# Guard自动恢复候选安装包

已构建完整自恢复待验收包，根目录安装及修复入口固定请求 **RecoverExited**。验收通过后会启用独立Guard执行任务；缺少报告时拒绝安装，不会自动改装成ObserveOnly。本包具备自动恢复的软件交付链路，但尚未完成wj-epb真实自动恢复验收，不宣称已经上线。

## 文件与身份

安装包：`D:\Github\wanxiang\EPBTest\artifacts\deploy\V3.0.0.0_d6f6bdc9c2dc_AUTO_RECOVERY_PENDING_ACCEPTANCE.7z`。

SHA256：`400102423484BE825154DB271F83AD072B63E0DEE0C5059C5095C70021D94289`。

34,331,915字节，155文件，7-Zip最大压缩率及完整性测试通过，附`.7z.sha256`。安装工具提交`d6f6bdc9c2dc93701f5d6eb79fcd112fbecbc1a2`；复用已经JXCQ验证的3.0.0.0/94a5439 Main、Supervisor、Guard和RecoveryControl，组件未重新编译或改写，基础身份保留在Base。工具提交与组件提交分别记入清单，不冒充全部组件由新工具提交编译。

## 本次完善

- 修复完整合包与一键安装固定为观察模式的问题：已验收完整包可传递RecoverExited，安装/修复后核对执行任务确实启用；错误台架或机器在修改Main前拒绝。
- 提供完整待验收自恢复安装包：报告校验放在现场启用之前，构建和分发不需要伪造现场结果。现场无需Git或源码目录。
- 安装入口校验完整包、真实报告、附件摘要、Main/Guard身份、机器及台架，再生成受验收约束的Guard安装材料；继续调用原Main安全安装器和Guard自动模式安装器。
- 保留新鲜授权、60分钟过期、人工停止优先、安全断能卸压、持久化收口、精确进程身份、接管互斥和提交对账。无独立断能证明的卡死强杀不开放，本包不选择RecoverStalled。

## 使用

完整解压后查看包内`自动恢复安装说明.md`。无现场报告时，可执行`Install-AutomaticRecoveryBundle.ps1 -Mode ValidatePackage`检查包，不启用任务。验收报告及附件放在安装包之外，将报告拖到根目录“一键安装正式版.cmd”或“一键修复.cmd”上。

报告仍需真实恢复、物理安全和人工面板停止优先等证据，并绑定Base中的确切组件。尚未部署新组件时，先在维护窗口按已有94a5439观察/受控验收流程取得证据，再使用本包启用自动恢复；不能把观察安装当成已经完成自恢复安装，也不能用单独一次口头安全确认代替整个报告。

## 验证及限制

本地：模式分发6/6；候选包4/4，包括包完整性、缺少报告拒绝、模拟报告不能用于本机、模拟材料生成后由真实Guard校验器复核。真实Guard基础包兼容/拒绝测试12/12。模拟报告绑定虚构机器，未用于安装、未操作硬件或创建任务。

最终同包JXCQ验证已通过：候选包4/4、模式分发6/6，路径含中文和空格。另实际执行根目录安装CMD且不提供报告，退出码1，明确提示缺少现场验收报告，前后MTTFTest任务名称与状态完全一致、MainCount=0。没有启用自动任务，没有安装观察模式。CMD通过WinRM隐藏进程执行，不声称现场人员实际双击或点击UAC。

证据：`artifacts/field-deploy-20260908-v3-round4/jxcq-automatic-d6f6bdc.log`、`jxcq-automatic-entry-d6f6bdc.json`。JXCQ同包路径为 `C:\RecoveryGuard-3.0-Validation\自动恢复 候选-d6f6bdc\package.7z`，SHA256与本机一致。基础94a5439包既有八入口安装、监督启动、正常关闭、卸载和回退验证继续有效；这不冒充本次自动模式在硬件上的验收。

wj-epb本轮未部署、未停止，继续旧版及既有巡检。当前无CAN硬件，不检测CAN。I0050液压恢复状态发布一致性仍为第二步。整体“现场自动恢复已完成”目标尚未达成。
