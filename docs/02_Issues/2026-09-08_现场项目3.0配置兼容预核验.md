# 现场项目3.0配置兼容预核验

## 历史数据库实际组件验证

以已核验的73,129,984字节历史index.db创建隔离副本，用c8daa03完整包真实DataOperation.dll构造并释放EpbDiskWriter，成功完成数据库打开、初始化及启动处理。环形数据使用夹具新建的小文件，因此本项只验证索引兼容，不证明历史原始采样恢复或续测。

原始数据库哈希未变；处理后integrity_check=ok。七个有历史记录的通道，其epb_cycles机械完成合计、最大圈号、行数与mechanical_baselines均保持一致，各状态行数也不变。

首次测试错误地预期cycle_receipts总数不变，断言失败。核对实际OpenRawJournal代码后发现其INSERT OR IGNORE会从epb_cycles补齐缺失回执；随后按主键逐行构建“原回执保留＋缺失循环补齐”的独立预期值，与新库的epb_id/cycle_number/mechanical/completed_at/status全字段精确一致。这不是新增试验圈数；旧版继续写循环表而未同步回执的差异在此被补齐。未放宽为仅比较总数。

证据：artifacts/ReleaseValidation-20260908/legacy-db-open.log、legacy-db-open-result.json、Test-LegacyDbOpen.ps1及compare-legacy-db.py。数据库打开与回执补齐有正向证据；当前现场一致备份、实际剩余圈数对账和带载续测仍须在切换时验证。

后续隔离验证：从新包原始安装器提取迁移函数，重定向ProgramData、LocalAppData和项目路径到独立夹具。现场快照缺少三项安全证明时被明确拒绝，检查点哈希不变；另建模拟安全分支后，六通道剩余数完整保留，authorizationMigrated/permitMigrated/nonceMigrated均false，旧检查点封存。证据为artifacts/ReleaseValidation-20260908/field-checkpoint-migration-result.json；模拟分支不代表现场安全确认。

已找到项目历史增量备份中的一致性数据库，captured_utc=2026-09-06T22:09:26.3345235Z、版本2.14.2.11。该副本可用于后续数据库兼容测试，不能代表当前现场圈数或作为直接回退数据源。

2026-09-08 16:22:41 +08:00，只读取得wj-epb项目10243-028的TestConfig.xml、项目检查点和用户检查点，三个小文件合计约28 KB。未操作现场硬件、服务、任务或数据库。逐文件读取不是跨文件原子快照，不认定为停机一致备份。

使用c8daa03候选包真实Config.dll，在本机32位Windows PowerShell调用ConfigLoader.LoadTest。配置副本读取后哈希未变；六个启用通道的TotalCount、RunCount及MechanicalCycleCount与原XML逐项一致。

| 通道 | 目标 | RunCount | MechanicalCycleCount | 目标减机械完成数 |
|---|---:|---:|---:|---:|
|4|200000|45747|46444|153556|
|5|200000|45726|46427|153573|
|7|200000|45637|46325|153675|
|8|200000|45671|46371|153629|
|9|200000|45661|46362|153638|
|12|200000|45642|46345|153655|

本次检查点RemainingFormalCycles与表中剩余数相符。两个完成计数字段本来就不同；本轮没有用一个覆盖另一个，也没有仅据XML认定数据库已对账。

检查点SchemaVersion=6、BuildVersion=2.14.2.11、Armed=true；MotorOffConfirmed、PressureSafeConfirmed和PersistenceDrained均为false。安装器源码Invoke-LegacyCheckpointSafeRollover要求三项证明或明确人工断能确认，否则拒绝迁移。现场正在运行时的这些值不能用来放行换包；本轮没有伪造安全确认或执行迁移。

结论：**配置格式读取和计数保留已取得正向证据；完整跨版本续测尚未通过。**仍需在一致性副本上核验实际数据库、圈数语义和迁移结果，并在维护窗口正常停止保存后重新核对安全状态。该结果不证明3.0解决I0050，不开放现场自动恢复。

证据（仓库相对路径）：

- artifacts/ReleaseValidation-20260908/field-small-snapshot/source-records.json
- artifacts/ReleaseValidation-20260908/Test-FieldConfigParser.ps1
- artifacts/ReleaseValidation-20260908/field-config-parser-result.json

Config.dll SHA256：DF1DC5E9872655456A1B37CE34F1BCAFDF1F0046193B4487833A64BEE649697A。首次测试脚本误写Config.ConfigLoader类型名，修正为实际全局类型ConfigLoader后通过；未修改产品代码。
