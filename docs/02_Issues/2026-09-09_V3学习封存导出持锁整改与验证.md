# V3 学习封存导出持锁整改与验证

## 当前交付状态（以本节为准）

- 已完成：5d2eb9b修复候选完整包、本地/Release回归、JXCQ真实封存路径回放、同一压缩包的安装/修复/启动/正常关闭/卸载/回退验证，证据已回传。
- 当前限制：Guard自动恢复入口具备RecoverExited/RecoverStalled能力，但未取得本次新组件对应的真实现场验收；JXCQ生命周期安装使用Base观察入口，不能写成自动模式现场通过。
- 待确认后执行：wj-epb真实自学习、正式业务提交与数据对账、实际NI/电源/液压条件下的安全恢复和面板停止边界验证。
- 用户已明确要求等待再次确认。此前自行部署授权不能用于本轮现场切换；没有运行中的现场部署任务。
- 下文保留构建过程记录，其中“正在运行/待验证”描述的是当时状态，最终结果见末节。当前没有遗留的构建或JXCQ安装测试进程需要继续等待。

本次本地交付复核再次确认压缩包34337864字节，SHA256为5069D402354465A22BDB0D8B6F2A1E840D7CD01CB029E2ADE987C5F0A900A130，与JXCQ实测包一致。不能据此宣告实施方案全部现场验收完成。

## 结论与范围

B轮现场的首个故障是持久化积压，恢复许可RunId不一致是后续第二个故障。本轮修复一条确实能阻塞同设备写入的生产路径：学习结束使用SealAndExportCycle，旧实现持有通道Gate完成CSV/BIN编码、发布和文件重读校验；设备批次写入又需要取得该设备所有通道Gate。一个通道慢导出会阻塞同设备其它健康通道。

现场时序与此机制相容，但现存快照没有完整捕获Dev1在途调用栈，因此不能把这条路径称为唯一现场根因。此前JXCQ探测只调用CompleteCycle，未覆盖实际学习封存导出，本轮已纠正该覆盖缺口。

## 已实施

- 锁内领取圈并复制样本；设置SealInProgress，保留当前圈身份，阻止重复领取和其它终态修改。领取后的新批次不能继续归入已冻结的圈；生产调用方仍须先通过既有TryWaitForCycleDurableCutoffAsync。
- 对独立样本副本执行文件编码、原子发布和校验时释放通道写入锁。未领取的同设备通道继续写入。
- 重新取得通道锁后验证语义、提交数据库终态，只有真实验证及提交成功才发布成功进度。失败路径仍封存为failed，不伪造成功或跳过耐久校验。
- 增加封存生命周期读写门。Dispose先拒绝新的封存申请，等待在途封存提交后再把存储置为已关闭；避免导出线程收尾时数据库或MMF先被释放。
- 同时保留上一轮Watchdog恢复许可RunId修正及生产准入回归。

## 验证记录

本地磁盘全套76/76通过：test-detached-seal-final.log。新增两个确定性并发回归：

1. 导出被人为阻塞时，同设备另一通道仍完成真实写入；冻结圈样本保持8，健康通道从8增加到28；重复领取拒绝；导出完成前成功进度为0，完成后为1。
2. 导出失败与Dispose并发时，关闭等待封存收尾；失败不发布成功进度，重新打开数据库确认learning_failed。这个测试最初暴露_disposed过早设置会阻止失败收尾，已用独立disposeRequested与最终disposed状态修正并验证。

JXCQ更新验证正在运行：C:\RecoveryGuard-3.0-Validation\DetachedSeal-B-20260909。先执行同一76项磁盘测试，再执行36轮/36,000批次、现场2+4通道分组、真实SealAndExportCycle的学习回放。结果需等待同一进程退出，不提前计为通过。

控制全套正在对本轮改动重新构建和执行。此文档创建时尚未生成新正式包，尚未再次部署wj-epb。既有现场仍保持旧版续测；无CAN检测，I0050仍为第二步。

证据目录：artifacts/field-deploy-20260908-v3-round4。

## 隔离复核终态

JXCQ同一新测试包76/76通过；真实SealAndExportCycle学习封存路径36轮、36,000批次完成，所有导出样本数断言通过，最长设备写入76.472ms，捕获9个超过50ms批次。结果见jxcq-detached-seal-B.log。本轮证明确定性慢导出并发场景及隔离回放通过；没有把它标为真实硬件自学习通过。

对照旧版标签后进一步确认：2.14.2.11也存在持锁SealAndExportCycle；因此持锁本身不是新版独有的代码。新增FULL原始日志及其提交/清理带来不同负荷，与旧有共用锁机制叠加的影响仍需现场验证。本次整改消除这个具体阻塞环节，不宣称已证明唯一根因。

干净发布源码已提交5d2eb9b，正在构建匹配Main组件。主工作区保留其它既有修改，未全盘提交或覆盖。完整包未产出前不替换现场运行。

## 完整修复候选包

发布源码：5d2eb9b273099775d5b19c9d3dfd6d4232b80b1e，干净构建。Debug控制805/805、磁盘76/76、电源19/19通过；正式Build-Release也完成其Release回归、49项现场门禁脚本测试和41项部署脚本测试并退出0。

推荐继续验证的完整自动恢复候选包：

- 路径：D:\Github\wanxiang\EPBTest\artifacts\deploy\V3.0.0.0_5d2eb9b27309_AUTO_RECOVERY_PENDING_ACCEPTANCE.7z
- SHA256：5069D402354465A22BDB0D8B6F2A1E840D7CD01CB029E2ADE987C5F0A900A130
- 7-Zip最大压缩，包含Main、独立Guard、一键入口、校验清单及说明。自动安装入口为RecoverExited，支持经额外验收的RecoverStalled。缺少对应真实现场验收报告时自动安装拒绝，不自动降级冒充已开启恢复。
- Base目录保留受控现场切换/观察安装入口。先观察安装不代表自动恢复验收完成；旧报告不能授权新组件身份。
- 本地候选安装契约6/6通过，全部为隔离合成测试，没有安装真实任务或产生硬件动作。

基础一键包：D:\Github\wanxiang\EPBTest\artifacts\deploy\V3.0.0.0_正式版_5d2eb9b27309_QUICKDEPLOY_GUARD.7z，SHA256：5E4C31FD56BAEAC522058C80B57DB4AB05A64247D1756E16DFA1CE41D3D25396。基础包Guard默认观察；不要将它与已启用自动执行混淆。

JXCQ最终同一压缩包正在传入C:\RecoveryGuard-3.0-Validation\学习封存 修复-5d2eb9b；传输后核对上述SHA256，再执行最终安装生命周期验证。此前76/76和36,000批次回放是修复二进制测试，尚不能代替最终包的一键安装/修复/卸载/回退实测。wj-epb此时尚未部署本包，保持旧版续测。

用户最新指示：wj-epb须等待再次明确确认后才部署。本轮未向wj-epb部署，后续仅继续JXCQ验证和本地交付整理。旧的自行部署授权不再作为本轮现场切换依据。

匹配发布源码的Guard契约全套97/97通过（test-guard-5d2eb9b.log）。最终自动恢复候选压缩包已在JXCQ核对SHA256一致；包内有顶层版本目录，已据实修正测试脚本路径。首次脚本验证被JXCQ执行策略阻止，未进入安装；随后仅对当前验证进程设置ExecutionPolicy Bypass，正在执行Base安装生命周期测试，未更改机器全局执行策略。自动安装入口仍要求真实现场报告，没有使用合成报告安装。

## 最终同包JXCQ安装生命周期验收（08:11—08:13，北京时间）

最终同一.7z包SHA256已在JXCQ核对一致。八个根目录操作入口退出码全部为0：一键安装正式版、检查运行状态、一键修复、恢复后台服务、一键故障采证、启动试验、一键停止全部相关进程、一键卸载。安装与修复使用该包Base的受控观察入口，未伪造报告启用自动执行。

通过交互桌面计划任务运行正常入口：Main PID23260，窗口标题“MT EPB常温疲劳测试 V3.0.0.0 [PID 23260]”，窗口可用；CloseRequested=true、Exited=true。这里验证的是监督启动和窗口正常关闭，不是实际硬件试验开跑。

卸载后回退JXCQ原2.17.3.0，程序SHA256与安装前完全一致，ProgramData配置哈希无差异，Supervisor Running，MainCount=0；新包安装后与原ProgramData配置哈希对比也无差异。临时UI验证任务已删除。

实测证据已回传：artifacts/field-deploy-20260908-v3-round4/jxcq-5d2eb9b-final-evidence，包括actions.jsonl、before/installed/interactive-result/rollback-result/rollback-check.json及各入口日志。

当前交付结论：完整修复候选包、本地回归、JXCQ写入回放和最终同包安装生命周期均已完成。真实wj-epb自学习、正式圈数推进和硬件条件下Guard异常恢复尚未验证，不宣称已彻底消除现场故障。按用户最新指示，必须收到新的明确部署确认才进入wj-epb；本轮没有部署或停止wj-epb。
