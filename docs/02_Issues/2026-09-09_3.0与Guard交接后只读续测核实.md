# EPB 3.0 与 RecoveryGuard：交接后只读续测核实

核实时间：2026-09-09 09:34（北京时间）。

## 结论

用户在 09:13 人工停止后自行重启的旧版试验已恢复实际推进。本次只执行两次只读快照，不发送启动、停止、部署、任务启用或硬件控制命令。

现场当前安装仍为 `2.14.2.11`，不是候选 `3.0.0.0`；`MTTFTestRecoveryGuard`、`MTTFTestRecoveryGuardExecution` 和 `MTTFTestRecoveryHealth` 均保持 Disabled。本记录不构成新版本部署或自动恢复验收。

## 两次只读快照

| 项目 | 第一次 | 第二次 | 结论 |
| --- | --- | --- | --- |
| 采集 UTC | 01:34:08 | 01:34:33 | 间隔约 25 秒 |
| 主程序 | `MTTFTest.exe` PID 24032，启动于 09:18:49 | 同一身份 | 用户重启后的实例仍存在 |
| Supervisor | Running | Running | 未观察到维护抑制 |
| 六通道 4/5/7/8/9/12 | 均为 `Running` | 均为 `Running` | 无人工停止残留状态 |
| 正式/机械圈 | 各通道分别为 49107/49911、49088/49896、49008/49804、49042/49849、49029/49836、49010/49820 | 各通道均增加 2 | 业务实际推进 |
| SQLite `epb_cycles` | 各通道最新提交为 01:33:54--56 | 各通道最新提交为 01:34:24--26，receipt 与机械计数均增加 2 | 持久化实际推进 |
| 读取错误 | 无 | 无 | 快照可用 |

旧版不支持 V1 健康管道，脚本将其标为 Unsupported；本次结论依据进程身份、通道状态和 SQLite 持久化交叉证据，而非把 Unsupported 判为故障。

原始快照位于 `artifacts/field-deploy-20260908-v3-round4/wj-incident0834-recovery/handoff-readonly/20260909-093408-783.json` 与 `20260909-093433-855.json`。它们由只读脚本采集，未包含 CAN 检测。

## 交付与后续门槛

- 发布候选源码已复核为干净提交 `5d2eb9b273099775d5b19c9d3dfd6d4232b80b1e`；当前任务工作树为不同提交，因此不以它重新打包或覆盖该候选。
- 既有 JXCQ 结果仍有效范围内：学习封存隔离回放、同包 SHA-256、一键安装/修复/启动/正常关闭/卸载/回退均完成；没有重复运行已通过的测试。
- `wj-epb` 的 `3.0.0.0` 切换仍须等待用户新的明确部署确认。确认前仅维持已有的 30 分钟只读监控，截止时间不变。
- 真实 NI/电源/液压条件下的新版本自学习、正式业务对账、Guard 异常恢复与面板停止边界均为未验收；I0050 液压恢复状态发布一致性仍是独立第二步，未在本记录中宣称完成。
