# JXCQ 无硬件模拟复核

## 15:55 最终包安装预演发现并修复问题

修复已纳入独立发布分支提交 `c8daa03`，并启动完整 Build-Release 流程，日志 `artifacts/ReleaseValidation-20260908/build-formal-whatif-fixed.log`；此处仅记录已启动，最终结果须以进程退出码及发布身份为准。JXCQ 已新增 `C:\RecoveryGuard-3.0-Validation\FinalPackage-Install-20260908`，保存当前服务定义、三项任务XML、配置副本及120个安装文件的哈希清单。监督服务仍运行，这些记录不等于监督状态的一致性备份，实际切换前仍需收口并补齐。

对最终交付包执行真实安装器 `-Mode Install -WhatIf`，Windows PowerShell 5.1 将内部 Get-FileHash 的 ProviderPath 查询也作为预演处理，导致第一个文件被误报为哈希不一致，退出码1。失败证据保留在 `artifacts/ReleaseValidation-20260908/jxcq-final-installer-preflight.json`。这是安装器预演缺陷，不是文件已损坏的证明。

源码安装器改为以只读 FileStream/SHA256 计算清单文件和身份文件摘要，保留外层 ShouldProcess，不关闭全局预演。最终源码脚本在 JXCQ 对同一不可变 Main 包预演通过，退出码0，输出仅为预期的安装动作提示；证据 `artifacts/ReleaseValidation-20260908/jxcq-final-installer-preflight-fixed.json`。本地 Windows PowerShell 隔离部署回归41/41通过，含新增预演真实校验与调用者偏好保留用例，日志 `artifacts/ReleaseValidation-20260908/installer-whatif-regression.log`。

此次未实际安装；JXCQ 已安装版本仍为2.17.3.0，监督服务Running。未操作wj-epb。修复尚未进入此前冻结的交付ZIP，后续必须重新构建并验证交付，不能把外置修复脚本的结果算作旧ZIP预演通过。完整安装、启动、回退以及现场原项目兼容验证仍待完成。

用户要求先在 JXCQ 无硬件模拟测试，再考虑 wj-epb 更新与现场跟踪。本轮未连接或修改 wj-epb，未检查 CAN，未访问 NI、电源或液压输出。

本轮在 JXCQ 独立目录执行 RecoveryGuardTests，91/91 通过，退出码 0。覆盖人工停止/暂停优先、59分59秒与60分钟过期边界、快照重放、旧授权拒绝、看门狗与 Guard 互斥、并发启动、冷却重试、执行者失联、恢复事务接续及业务提交验证。时间边界采用模拟时钟，不代表实际断电或持续运行60分钟。

证据位于 `artifacts/RemoteGuardNoHardware/`：`latest.json` 记录远程路径、测试二进制哈希，`rerun.log` 为实际测试输出，`exit-code.txt` 为退出码，`after.json` 为测试后环境状态。首次后台启动未产生测试日志便退出，不计为通过；随后保持 WinRM 调用执行完整测试，以上结论只来自第二次完整输出。

本轮没有卸载或替换 JXCQ 原程序。测试后 Supervisor Running、主程序0、Guard任务0、测试进程0。既有 SYSTEM 双任务整链10/10证据来自此前 `artifacts/RemoteGuardSystemTaskE2E/20260908-124303-pass`，本轮未重复该安装态场景。

判断：本轮覆盖范围内未发现新的失败，可以继续准备匹配3.0.0.0整套业务组件及原项目副本的兼容验证。当前结果不能证明完整生产主程序长稳运行无重大问题，也不能替代真实硬件安全和圈数对账。wj-epb 正式试验运行期间保持原程序；后续更新需在维护窗口执行正常停止、保存、备份、切换及续测对账。现有 ObserveOnly 包不会自动续测，不能直接挂接旧版程序。

## 完整包准备阶段的新增验证

用户进一步要求先完成 JXCQ 模拟并生成正式完整包，再通知安排 wj-epb 现场运行监控。本阶段不连接或部署 wj-epb。

- 修正 I0049 长稳测试报告写死的旧版本号，改为读取 Controller 实际程序集版本。这不改变业务控制行为，也不代表本阶段执行了六小时测试。
- 本地及 JXCQ Debug 恢复接入回归均为 39/39：Supervisor 16、SafetyAgent 对账 10、主程序桥接 9、创建回执对账 4。
- JXCQ 六通道、每通道 2 kHz 模拟生产和真实写盘 60 秒通过，每通道 120,000 个样本。首次测试目录遗漏 SQLite 原生依赖而失败，补齐 x86/SQLite.Interop.dll 后通过；首次失败保留在 persistence-missing-dependency.json，不计入通过。
- 原先按整个 Debug 目录传送的操作因包含历史 DataStore 文件而主动中止，不计为一次测试；后改为明确选择 EXE、DLL、配置和所需原生依赖的隔离输入包。
- 为保留原工作区，创建独立源码快照分支 codex/release-v3.0.0.0-20260908，首个快照为 8ebdb4cf61a339e329839cf6b9556c246162290b。该快照 Release 组件在 JXCQ 再次通过恢复接入 39/39 和 60 秒写盘。证据 formal-result.json 记录主程序、Controller、测试程序哈希；这里的 formal 文件名仅指正式包准备阶段，不能替代发布校验通过标记。
- 完整发布流水线发现部署契约测试仍要求 SessionAgent schema 7，而代码已经为 schema 8。已修正这一版本断言及错误提示，其余安全门禁断言保留。发布流水线须在修正后通过，不能把此时产物作为正式包放行。

新增证据位于 artifacts/RemoteGuardNoHardware/，构建和发布检查日志位于 artifacts/ReleaseValidation-20260908/。60 秒负载测试不等于整机长期运行验证；以上接入测试使用模拟安全边界和隔离子进程，不证明现场物理断能。最终包及校验结果将在通过后单独补录。

## 发布门禁发现并修复的问题

干净快照暴露了原工作区已有 Debug 文件掩盖的测试依赖：Test-V217Deployment.ps1 固定读取 Debug，而 Build-Release.ps1 只构建 Release。现已增加显式 Configuration 参数，正式流水线传入 Release。测试清单不再写死 2.17.3.0，改为读取实际主程序版本。

进一步验证发现主安装器只接受 2.17 和八组件清单，确实会拒绝 3.0.0.0 完整包。已明确允许 3.0.0.0 的九组件集合和 SessionAgent schema 8，保留 2.17 原架构版本准入，并新增组件名集合和重复校验。没有放开其他版本或 EPB-V3 EngineHost/RecoveryKernel 架构。新增拒绝用例覆盖旧协议、缺失 RecoveryControl 和重复组件；本地完整隔离换包测试 39/39。

JXCQ 全套换包测试的权限注入前提不成立：对临时目录设置拒绝 ListDirectory 后，Get-ChildItem 和 .NET Directory.GetFiles 均仍可读取。故全套远程失败记录保留在 slot-result.json，不报告远程39/39。单独生成 Test-SlotOnly.ps1，明确移除这两个环境相关采证用例，其余同源换包/维护/SCM身份模拟用例在 JXCQ 通过37/37；slot-focused-result.json 记录脚本哈希和 SkippedAclChecks=2。本地完整门禁保持原样，不跳过该权限测试。

JXCQ 首次脚本执行被执行策略阻止，后续使用仅对子进程生效的 PowerShell 执行参数运行已核验测试脚本，未修改系统执行策略。中止传送留下的 Runtime-NoHardware-20260908-141532 临时目录清理被自动审批拒绝（仅返回 blocked by policy）；该目录保留，不参与交付或部署。

## 最终交付结果

最终干净提交为 c795442ab12048ba3ad9f7b80075392a2045cbd7。完整正式流水线通过控制801/801、写盘70/70、电源19/19、现场门禁脚本49项、隔离换包39/39及部署契约。此前失败的构建日志保留，不计入最终通过；最终完整日志为 artifacts/ReleaseValidation-20260908/build-formal-verified.log。

最终完整压缩包在JXCQ通过SHA256复核、统一只读校验、实际安装器准入函数校验和包内组件恢复接入39/39，退出码均为0。包SHA256为6E730D68F7116C13C338573DB21FF739FCD42F35406F802EF68EFAFA412C205E；主程序SHA256为433A039933612616C20071C866181B0EA77A321CDCA618A7B5BE081C6C41B707。最终证据是 artifacts/ReleaseValidation-20260908/jxcq-delivery-result.json；此前formal-result和final-runtime-result属于较早源码身份，不冒充最终主程序的哈希证据。

完成范围为3.0.0.0完整主程序正式包及匹配独立Guard观察包，自动执行禁用。没有在JXCQ启动完整生产主程序或安装整套现场环境；没有连接wj-epb。完整包、校验值及现场切换步骤见[交付与现场切换](../RecoveryGuard_3.0.0.0_完整包交付与现场切换.md)。
