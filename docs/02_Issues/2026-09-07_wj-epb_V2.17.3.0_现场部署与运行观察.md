# wj-epb V2.17.3.0 现场部署与运行观察

## 当前结果

2026-09-07（北京时间），用户授权开始：正常停止 V2.14.2.11，封装数据，回传与清理项目日志并行，再部署 V2.17.3.0 并恢复项目。用户明确取消回传后的摘要及解压复核。

**V2.17.3.0 已实际安装并尝试续测，但现场验收失败；已回退 V2.14.2.11 并验证六通道正式运行和数据提交继续推进。24 小时只读监控已启用，监控当前回退后的旧版。** 新版首次启动发生持久化拥塞，Supervisor 完成进程替换后，学习转正式阶段再次发生 Timer/执行许可故障。所有旧数据及新版学习记录均保留，没有清零或重建项目。以下保留操作时间线，末尾追加最新运行观察结果。

## 现场身份与安全停止

- 连接入口：本机 PowerShell `Get-WjEpbSession`；连接名 `wj-epb`，实际计算机 `MT-20250724OGLX`，账户 `MT-20250724OGLX\mt`。未记录认证秘密。
- 旧主程序：`C:\Program Files (x86)\MTTFTest\Current\MTTFTest.exe`，V2.14.2.11，PID 14084，启动时间 2026-09-07 06:11:04（现场时间）。
- 项目：`D:\EPB_Data\10243-028`，目标 200000 圈，配置周期 15 秒，启用通道 4、5、7、8、9、12。
- 通过现场交互会话中经过进程、控件文字及句柄核实的“停止试验”控件发起正常停止。约 18:32:20 的当前会话回执确认 MotorsOff、PowerOff、PressureSafe、PersistenceDrained、LogicalQuiescent、DataContinuityVerified 全部为 true；CloseIntent=ManualStopIntent。
- 随后正常关闭实时监视及主窗口，`close-result.txt` 记录 Closed normally；主程序 PID 14084 已退出，没有使用强制终止。
- 当前会话：`742ee4c43338404cb9b64e2ccb440446`；停止事务：`97a77a9ddcf141cf86063a356b118f79`。

## 停止后进度基线

以下取自停止后项目 TestConfig.xml。RunCount 与 MechanicalCycleCount 分开保留；剩余数按目标减 MechanicalCycleCount 列出，不能将两种计数强行改为一致。

| 通道 | RunCount | MechanicalCycleCount | 剩余机械圈数 | 状态 |
| --- | --- | --- | --- | --- |
| 4 | 40630 | 41306 | 158694 | ManualStopped |
| 5 | 40612 | 41290 | 158710 | ManualStopped |
| 7 | 40517 | 41184 | 158816 | ManualStopped |
| 8 | 40551 | 41230 | 158770 | ManualStopped |
| 9 | 40540 | 41220 | 158780 | ManualStopped |
| 12 | 40522 | 41204 | 158796 | ManualStopped |

数据仍在原项目目录。index.db 观察大小为 77602816 字节；封装快照时 WAL 为 0 字节。此文件元数据不替代后续实际数据库及续测对账。

## 封存、回传与日志处理

使用现场原脚本 `D:\EPB_Data\EPB-IncrementalBackup_new.ps1`，明确指定项目、旧版本和旧 EXE 路径。首次因 PowerShell 执行策略受阻，按原文档仅对当前进程设置 Bypass 后成功执行；未修改机器级策略。

- 现场封存包：`D:\EPB_Data\Backups\10243-028_V2.14.2.11_I0051_0907_1834.7z`。
- 本次节点：incremental 0051，ID `69eee8868d51452fbd1e61102c8856bd`；基包 ID `b8cfd6848e634c5aaed3285e44af2b1c`；前一节点 ID `74cb4e3a6c46497c8fd0581906e55cf7`。
- 原始变化量约 0.38 GB，归档 30144888 字节，压缩耗时 1 分 21 秒。原脚本自带的 7-Zip 检查成功；没有修改脚本取消其内部检查。
- 回传目录：本机 `D:\EPB_Data\Backups`。Robocopy 对项目匹配的 51 个归档执行复制，1 个新增、50 个跳过、0 个失败，退出码 1 表示复制成功；完成时间 18:38:03。未额外计算回传包摘要或解压验证，也未对全部历史链内容进行独立验证。
- 自动审批审查拒绝递归删除项目 log，原始原因仅为“blocked by policy”。改用可逆归档：将准确路径 `D:\EPB_Data\10243-028\log` 移到 `D:\debug\CodexDeploy_20260907\ArchivedProjectLog`。移动前核实源目录与目标根的绝对路径及非重解析目录属性，目标不得已存在，且主程序已退出。原日志位置已腾出，日志原件未删除；其他项目数据和事故目录未清理。

## 安装包、备份与回退准备

本机发布包为 `artifacts/deploy/V2.17.3.0_正式版_0d80572f5fe7_QUICKDEPLOY_R30.7z`；本机与现场安装包 SHA-256 一致：

```text
fc44031b485c2628c83d2cb2d4973be790dc7bcf2c5dd4ae7056307e7c72b449
```

现场部署工作目录：`D:\debug\CodexDeploy_20260907`。

| 内容 | 现场路径 |
| --- | --- |
| 完整旧程序副本 | `D:\debug\CodexDeploy_20260907\RollbackCurrent` |
| ProgramData 配置副本 | `D:\debug\CodexDeploy_20260907\RollbackProgramDataConfig` |
| 停止后项目配置 | `D:\debug\CodexDeploy_20260907\TestConfig-stopped.xml` |
| 旧服务配置记录 | `D:\debug\CodexDeploy_20260907\service-before.json` |
| 原登录代理/主程序任务定义 | 工作目录内 `MTTFTestSessionAgent.xml`、`MTTFTestAutoStart.xml` |
| 新包传输副本 | `D:\debug\CodexDeploy_20260907\V2.17.3.0_R30.7z` |
| 新包解压目录 | 工作目录下 `NewPackage\V2.17.3.0_正式版_0d80572f5fe7_QUICKDEPLOY_R30` |

18:37:41 通过包内 `一键安装正式版.cmd` 启动安装，设置其既有 NONINTERACTIVE 标志防止末尾 pause。安装器报“仍有安全接管或旧会话，先执行一键停止全部相关进程，再安装”。未绕过检查，未使用会强制结束后台进程的清场工具。

本次未换包，无需恢复 Current。后续若换包后失败，应先正常停止并保留新版证据，再按受支持的安装流程恢复旧程序及匹配配置；禁止覆盖最新项目数据或回放旧恢复许可。完整旧版回退安装与服务恢复尚未实测，不能记作通过。

## 已捕捉的部署阻塞异常

残留进程为 `C:\Program Files (x86)\MTTFTest\Current\MTTFTest.Watchdog.exe`，PID 21052，启动时间 2026-09-06 22:25:24。命令行为 `--session-host --parent-pid 17896`，会话 `e1e39a252d7a4937bf501091092826b1`。对应旧主程序 PID 17896 已不存在；本次停止的主程序 PID 14084 也已退出。

历史会话的关闭记录仍为 ApplicationClosing，保留恢复许可，LogicalQuiescent=false，IsSafetyTerminal=false，终因 SafeButRestartRequired。它与本次六项安全证据全部通过的当前会话不同，不能用当前回执改写历史会话记录或宣称旧会话已经逻辑收口。

已确认影响：残留会话触发安装器 Assert-RuntimeIdle 门禁。尚未确认残留根因。用户原要求禁止主动强杀，因此若无受支持的正常退场方式，需要单独取得结束该精确残留进程的授权；不得按名称批量结束进程或改写监督文件伪造完成状态。

## 证据与后续验收

本机轻量证据目录：`artifacts/field-deploy-20260907`，包含停止前检查点、停止后配置、安全回执、正常关闭结果、安装与封装输出、旧服务状态、日志归档记录及残留会话信息。完整数据封存文件不提交仓库。

- [ ] 清理残留监督会话并通过安装器门禁。
- [ ] 安装后主程序及各监督组件身份核验。
- [ ] 原项目各通道计数、采样、控制步骤和连续两次业务提交对账。
- [ ] 设置停滞和保存判据，启用每五分钟、24 小时只读监控；正常静默，异常及恢复状态变化通知。
- [ ] 实际运行观察、异常恢复结果及24小时总结。
- [ ] 未自然发生的安全或恢复场景继续标记未验收，不主动故障注入。

辅助交互任务 `EPB-Codex-DeploymentUI` 仅用于本次正常停止和窗口操作，不是运行监控任务。尚未启用自动续测或24小时远程监控。

## 18:42 后续处理：残留清理完成，迁移安全标志阻塞

用户明确授权结束精确残留 Watchdog，并授权后续同类残留处理。适用条件是先核对路径、PID、启动时间、关联会话及旧主程序已退出；不扩大为运行中主程序强杀、硬件故障注入或绕过安全门禁。

本次复核 PID 21052 的路径、启动时间和命令行、父 PID 17896 已不存在、当前主程序不存在后，结束该精确进程并确认退出。证据为 `residual-cleanup.json`。

18:42:21 第二次安装通过残留门禁，正常停止旧监督服务和运行任务，安装器结束 SessionAgent PID 18120；随后在第 3/9 步拒绝迁移：`schema 6 会话缺少 MotorOff/PressureSafe/PersistenceDrained 三项安全证明；保持 SafeIdleAlarmed，拒绝安装新授权。`

对比证据：

- 当前会话 `stop-safety.json` 中 MotorsOff、PowerOff、PressureSafe、PersistenceDrained、LogicalQuiescent 全部 true。
- `%LOCALAPPDATA%\MTTFTest\unattended-run-checkpoint.json` 的 Revision=22877、LastReason=MonitorClosing、Armed=false，但 MotorOffConfirmed、PressureSafeConfirmed、PersistenceDrained 仍为 false，仍带项目及剩余圈数；对应证据保存为 `migration-blocked-checkpoint.json`。
- 安装器仅接受检查点三项标志或用户明确的 `PhysicalIsolationConfirmed`，没有读取当前会话停止回执的入口。因此此次阻塞不能推断为本次断能或保存实际失败，也不能把其他回执存在当作安装器已接受迁移。

原 Current 尚未替换、仍为 V2.14.2.11；现场保持停机，监督服务与原运行任务已被安装器停止/禁用，维护标记保留。最新状态见 `migration-blocked-state.json`。未篡改检查点、未声明未经人工确认的物理隔离、未重建或清零项目。

继续路径：取得真实现场物理隔离确认后，使用安装器已支持的物理隔离参数继续迁移，保留当前软件断能和保存证据；否则须修复并验证安装器消费精确停止回执的兼容路径，或制定恢复旧版监督配置的回退操作。不得直接设置检查点安全布尔值为 true，不能仅为通过门禁移动/删除该检查点。

## 18:45 新版安装完成与实际续测失败

用户在物理断能隔离确认问题后明确回复“确认安全，后续类似情况直接执行”。18:45:02 使用原包安装器的 `PhysicalIsolationConfirmed` 参数继续，9/9 步完成，Supervisor、SessionAgent 和分钟健康任务安装成功。迁移只保留剩余圈数，旧授权、许可和 nonce 未迁移；归档位于 `C:\ProgramData\MTTFTest\MigrationArchive\legacy-checkpoint-20260907T104503675Z`，项目旧监督目录封存为 `WatchdogSessions.LegacySealed-20260907T104503675Z`。

新主程序 PID 10324 打开原项目，六通道机械圈数与停止基线完全一致。18:47:38 发起开始试验，进入学习，未手工改写进度。

| 时间（北京时间） | 实际异常与恢复证据 |
| --- | --- |
| 18:48:04.281 | Dev2 持久化队列达到硬上限 256 批，锁存安全暂停。随后 DAQ 后台最老批次超过 500 ms 时效门限。 |
| 18:48:05.465 | 学习执行重建达到局部自愈阈值，升级整批安全重建。 |
| 18:48:08.845 | 同进程恢复因 RequiresRestart=true 清场结果失败，升级进程恢复。 |
| 18:48:19.450 | 出现 TakeoverExitBlockedWithoutApprovedPermit；后续监督继续交接，不将此中间错误单独记作最终恢复失败。 |
| 18:48:42 | Supervisor 自动启动替代主程序 PID 18512，启动 UTC ticks `639243749220359720`。未由远程手动重启。 |
| 18:48:47 | 替代主程序通过预检，以原项目六通道及原剩余圈数重新学习。 |
| 18:51:27.064 | 学习转正式时 EPB12 报 TimerNotRunning，Timer 状态为 Created。 |
| 18:51:27.382 | 监督记录进程交接启动确认；该确认不足以证明正式业务持续推进。 |
| 18:51:31.168 | EPB12 自愈因 PowerEnableRejected StaleExecutionPermit 达到阈值，再次整批重建。 |

连续只读 SQLite 快照证实学习圈和采样数据有提交，例如约 18:51:24 的学习圈 sample_count 为 14822 等；RunCount 尚未增长。恢复 manifest 虽为 RecoveryBatchCommitted，但不满足正式圈持续推进验收，因此按章程回退，没有把窗口出现或恢复提交消息当成成功。

## 新版安全停止、证据封装与旧版回退

18:52:05 通过停止试验按钮发起 ManualUi 事务 `5343aaf723324f3f834f5028c00f180e`：四组电源关闭，两路液压确认低于 5 bar；Dev1 Published=Persisted=19553、Dev2 Published=Persisted=19537，队列为 0，RawDrained/PersistenceDrained=true，无丢弃。

18:52:08 主程序记录“已安全收口”，但安全交接提交失败；另有停止后访问已释放 PowerSupplyCoordinator 的后台异常。18:53:37 正常关闭请求复用停止凭证后卡住，等待 60 秒仍未关闭。这是本次捕捉到的另一项现场关闭问题。

根据用户对后续同类安全停止后残留处理的授权，先保存完整日志、配置及监督记录，核实本次电源、卸压和持久化证据，再进入维护状态、停止监督服务及任务，按精确路径和启动 ticks 结束残留主程序 PID 18512、sidecar PID 9508。这不是故障注入，也没有在试验运行中直接强杀。证据位于 `rollback-residual-cleanup.json`、`FirstStartFailure\FinalLogs`。

新版数据已通过原脚本封装为 `D:\EPB_Data\Backups\10243-028_V2.17.3.0_I0052_0907_1855.7z`（16893666 字节），节点 ID `268aafc4e02a464e9931c23c45557484`，前节点为 I0051。18:57:38 回传本机 `D:\EPB_Data\Backups` 完成，Robocopy 1 文件成功、0 失败，未额外校验回传包。

回退步骤与结果：

1. 保留新版程序为 `C:\Program Files (x86)\MTTFTest\FailedV2173-20260907`，不删除新版安装或数据。
2. 归档新版分钟健康任务定义到 `V2173-RecoveryHealth.xml` 并移除该任务，避免旧组件不支持新健康协议时反复触发。旧版没有该独立分钟任务，属于回退后的已知能力限制。
3. 旧版 Install 入口因 `Stop-ScheduledTask -Confirm` 不受支持而报错；当时无生产进程运行、Current 已归档。改为复制完整 RollbackCurrent 到空的 Current，再运行旧版受支持的 Configure 入口，重新配置服务和任务，保留当前 ProgramData 配置。
4. 回退 EXE SHA-256 为 `9158a6cf57cfd6329d3f8dc3dd254f5e35a0d8c8b9fe48cd43b9a3919837a8ba`，与升级前一致。
5. 旧版不支持新版 `--launch-main` 入口，首次此入口启动失败且没有主程序。随后使用旧版既有 MTTFTestAutoStart 任务启动主程序 PID 20744，现场时间 18:58:59，打开同一项目。

回退后机械圈数为 41317、41301、41195、41241、41231、41215，分别比最初基线增加 11；这些是新版实际执行并提交的学习机械圈，原 RunCount 保持不变。没有回写旧备份覆盖这 11 圈。旧版读取结果与 SQLite 一致，正式运行继续核验。

## 只读巡检工具

新增 [Read-WjEpbFieldStatus.ps1](../../Tools/Read-WjEpbFieldStatus.ps1)：读取精确进程身份、监督状态、任务结果、项目计数、SQLite 提交及最近带采样数据的记录；新版使用独立只读健康管道，旧版明确标记不支持，不伪报健康。文件读取采用 ReadWrite/Delete 共享和有界尾部，SQLite 以 Read Only 打开、查询超时 2 秒，远程作业等待预算 70 秒；仅在本机证据目录写快照。

已在新旧版本现场实际执行，SQLite 及状态读取均成功。健康任务/代理返回值结合状态解释：常驻代理重复触发时的 AlreadyRunning 结果不能独立当作退出或恢复失败。采集本机与现场墙钟存在数秒差异，事件排序优先用现场时间、PID/启动 ticks 和业务提交，不将采集文件名当作现场精确时间。

旧版实际写入 `epb_cycles`，新版写入 `cycle_receipts`；回退后后者仍保留但不再更新。巡检工具已按版本选择实际持久化表，不能依据旧版不更新的新表误报保存停滞。运行期间 XML 和数据库采样并非同一原子快照，短暂一圈差异须复核。

## 19:04 回退验收与24小时监控起点

主程序 PID 20744，Supervisor PID 20928，SessionAgent PID 13104。19:03 和 19:04 两次独立快照确认六通道均为 Running，RunCount 各增长 3～4 圈，SQLite 对应记录和机械圈计数继续增长。正式圈记录具备 completed 状态、非零 sample_count 和持久化序列，例如 EPB4 正式圈 40716 的 sample_count=19380，EPB12 正式圈40585的 sample_count=18500；不是仅凭窗口判断。

| 通道 | 19:04 RunCount | 机械圈数 | 剩余机械圈数 | 状态 |
| --- | --- | --- | --- | --- |
| 4 | 40636 | 41333 | 158667 | Running |
| 5 | 40618 | 41317 | 158683 | Running |
| 7 | 40523 | 41211 | 158789 | Running |
| 8 | 40557 | 41257 | 158743 | Running |
| 9 | 40546 | 41247 | 158753 | Running |
| 12 | 40527 | 41231 | 158769 | Running |

数据位置仍为 `D:\EPB_Data\10243-028`。本次机械圈数包含实际重新学习产生的机械圈，RunCount 和 cycle_number 是不同口径，均原样保留，不做人工一致化处理。

- 本机自动任务：`wj-epb-24`，名称“wj-epb现场运行24小时只读监控”，heartbeat 已注册 ACTIVE，每五分钟一次。
- 观察起点：2026-09-07 19:04:02.7985342 +08:00；计划结束：2026-09-08 19:04:02.7985342 +08:00。到期后的首次唤醒只写总结和暂停任务，不继续读现场。
- 首次人工触发的同一巡检脚本已成功执行；自动任务已注册并读回确认，编写本段时尚未等待第一次定时唤醒，不能把注册成功写成已经持续监控24小时。
- 基线与窗口记录：`artifacts/field-deploy-20260907/monitor-window.json`，基线快照文件为 `monitor/20260907-190357-959.json`（现场时间以内容 UTC 字段为准）。
- Running 时持续无进度且最后提交超过120秒，间隔10秒复核后报警；学习允许10圈约150秒及启动余量，超过300秒无相应推进或重复学习却无正式进度时报警。新故障、异常退出、恢复失败或失联立即在检查发现时通知；同一状态正常静默并去重。
- 旧版无 V1 健康接口及独立分钟健康任务，保持明确限制。即时自动恢复由现有旧版 Supervisor 负责；远程巡检不重启、不清理残留、不部署或操作硬件。人工终态和急停仍遵循章程。
- 本次临时交互任务 `EPB-Codex-DeploymentUI` 已移除，辅助脚本及证据保留。

仍未完成：V2.17.3.0 正式带载持续运行验收（本次失败）、新增故障的根因修复及复测、回退后完整24小时观察。不能把旧版恢复运行写成新版验收通过，也不能承诺无中断或无数据损失。

### 首次定时巡检实测（19:12）

2026-09-07 19:12:28 +08:00 完成首次定时只读采证，快照 `artifacts/field-deploy-20260907/monitor/20260907-191223-462.json`。主程序、Supervisor、SessionAgent 及当前 sidecar 的 PID/启动时间/路径均未变化；六通道均 Running，较19:04基线各增加34个正式圈和34个机械圈，机械计数与 SQLite 相符，最新提交距快照约2～4秒，最近36条完成圈均有非零采样。

本段期间未发现新 ERROR；峰值软预警未达永久报警线且业务继续，按约定仅记录、不通知。首次定时样本距基线505.533秒（8分25.5秒），该间隔不能称为持续观测或严格每5分钟已有样本；保留于 `monitor-observations.jsonl`，24小时总结应统计实际采样间隔。巡检未修改现场程序、配置、服务或硬件。

## 监控授权更新（2026-09-07）

用户将巡检周期改为30分钟，随后明确授权：自恢复不成功，定时任务介入恢复。自动任务 wj-epb-24 已更新为监控与受控恢复，结束时间不变。执行条件及限次见章程第7节：本次事故安全/数据条件通过、Supervisor已失败且不并发接管时，最多一次远程恢复；人工终态、急停、安全或保存证据缺失均阻断。当前仅更新授权与任务，未主动触发故障或重启正在运行的项目；远程恢复故障实测仍未验收。


## 23:23 巡检：旧版 Dev1 采样空窗，现场已恢复

事件编号：acef6667a6074801878ffa12956106bb-20260907T150554-Dev1。首次发现23:23:01.120，上一有效正常快照22:18:00.935，实际间隔3900.185秒（65分钟）；不能记为严格30分钟采样。原主进程20744（V2.14.2.11，启动UTC10:58:59.712111）、Supervisor20928、SessionAgent13104及sidecar14816身份未变，RunId=a9b2e4fda5624faeb2b25b356e1e34b4，RunEpoch=1。

23:05:54.765–.769，EPB5/4因DaqSampleStale>250ms执行断电写入，回调年龄约269ms。23:05:55.687记录Dev1回调空窗1172ms、GapEvent=8。该行Energized=false是断电之后的观察，不能据此声称整个空窗都未带电。两路周期41687失败并按日志封存，避免周期尾重复记账；没有主进程退出。23:06:09两路下一正式圈41688成功（EPB5为SuccessWithWarning），23:06:24–25再完成41689。现场执行流程已恢复；Supervisor RecoveryAttempt=0，不能写成Supervisor换进程恢复。

23:23快照六通道Running；较22:18，EPB4/5正式与机械各增加259圈，其余四路各260圈，与两路各一次失败相符。六路XML机械数与旧版实际写入epb_cycles相符，最新提交距快照5–7秒；监督无活动恢复/永久报警，两卡队列0。现场已恢复，不满足远程介入条件，本次未发启动、停止、强杀或恢复命令。

证据：本机artifacts/field-deploy-20260907/monitor/20260907-232256-309.json；incident-230554.json为只读补采23:05–23:06相关run/warning/error日志选摘，共248条匹配、未超过500条上限，不等于完整日志。原始快照及事件记录保留，当前任务通知故障及已恢复结果。回调空窗底层原因仍未查明，后续版本应针对带电时样本陈旧、失败圈封存及下圈恢复补充验证。

## 2026-09-08 09:39—09:44 巡检：三起历史故障已在现场恢复，存在监控缺测

首次发现时间09:39:50 +08:00，上一有效快照03:34:34，两次实际间隔21915.680秒（6小时5分15.680秒）。这段时间没有有效巡检快照，不能称为按30分钟持续监控；下列故障均为本次补采发现，未能当时及时通知。调度/执行缺测原因尚未确认，不以日志补采替代实时监控。

身份仍为MT-20250724OGLX，V2.14.2.11，主程序PID20744及启动时间UTC2026-09-07T10:58:59.712111Z未变；Supervisor20928、SessionAgent13104、sidecar14816未变。项目D:\EPB_Data\10243-028，Session=acef6667a6074801878ffa12956106bb，RunId=a9b2e4fda5624faeb2b25b356e1e34b4，RunEpoch=1。

| 现场时间 | 故障证据 | 现场恢复证据 |
|---|---|---|
|04:11:18—19|Dev1回调年龄271.6ms，EPB5触发MotorOffAndAbortCurrentCycle；周期42909失败并封存，跳过重复记账|04:11:39周期42910为SuccessWithWarning，04:11:55及后续正式圈继续|
|04:31:41|EPB5周期42990出现PeakCaptureInvalid，CutoffNotCovered，Drain258.4ms超100ms，OffConfirmed=True，撤销执行权并进入隔离恢复|04:32:10周期42991为SuccessWithWarning，04:32:25周期42992成功，后续继续|
|08:28:48—58|H1样本年龄1123.1ms触发HydraulicSampleStale，压力70.176bar高于最低60bar，直接触发条件为陈旧而非低压；EPB4/5代次3235作废。随后三条WatchdogTransportLost，原因命名管道写入超时|08:28:58.679液压组1软件自愈完成Attempt=1，作废圈不计数；08:29:09/10两路正式圈43939成功，后续持续。监督会话AttachedReconnect、SameAuthority、ConnectionGeneration=5，RecoveryAttempt=0|

08:28期间Dev1记录1169.8ms回调空窗，后续两卡还有约534—642ms空窗。Energized=false是相应日志时点的状态，不证明整个空窗均未带电。采样延迟及通信写超时的共同底层原因尚未定位。以上均无主进程更换，不能写为Supervisor重启主程序恢复成功。

09:39六通道均Running，较03:34正式圈分别增加1459、1457、1460、1460、1461、1461；机械增加1459、1458、1460、1460、1461、1461。保留原始计数口径，不人工改平。09:43复查EPB9数据库比XML多1圈，属于分次采样待核差异；09:44再次复查六路机械数均与epb_cycles一致，最新提交距快照5—7秒，六路正式圈较09:39均再增加18，证实实际推进。

|通道|09:44正式圈|机械圈|剩余次数|
|---|---:|---:|---:|
|4|44154|44851|155149|
|5|44134|44834|155166|
|7|44043|44731|155269|
|8|44077|44777|155223|
|9|44067|44768|155232|
|12|44048|44752|155248|

现场已恢复，不满足“自恢复失败”介入条件，本轮没有远程启动、停止、强杀、清日志或修改试验数据。自动恢复介入仍受章程第7节的当次安全/保存证据和同一事故一次尝试限制。远程恢复路径故障实测、底层异常根因及监控缺测原因仍未验收/未解决。

本机证据位于artifacts/field-deploy-20260907：monitor/20260908-093952-791.json、20260908-094327-103.json、20260908-094418-674.json；incident-20260908-082848.json为相关日志筛选500条、未达到1000条上限；incident-20260908-early.json为04:11/04:31相关窗口252条匹配中保留150条（明确截断）；incident-20260908-0431-recovery.json补充04:31—04:32正式圈恢复记录。日志以共享只读方式读取，以上为筛选摘录而非全量日志，原始现场文件未改动。
