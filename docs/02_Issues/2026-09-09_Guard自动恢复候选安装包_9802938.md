# Guard 自动恢复候选安装包 9802938

版本 3.0.0.0；安装工具提交 98029388c12c755d6a16bcac9ccb06a43f6b8418；Main/Guard 基础组件仍为 94a5439b0d8f，运行时未重编译。

修复默认保留已安装 RecoverStalled/RecoverExited；显式模式仍须验收报告批准。缺失或未知 Mode 的已有设置拒绝覆盖。首次安装默认 RecoverExited，可显式选择 RecoverStalled。真实验收报告、安全证明、人工停止优先、60 分钟过期及接管互斥继续生效。不检测 CAN；I0050 留到第二步。

## 安装包

路径：`D:\Github\wanxiang\EPBTest\artifacts\deploy\V3.0.0.0_98029388c12c_AUTO_RECOVERY_PENDING_ACCEPTANCE.7z`

SHA256：`F9062BAB0D6685034DCBBBBFB60E64E96EAB1E196AB1CC3D77C193845FAFF7E4`

大小：34,331,995 字节，155 文件。7-Zip mx=9 构建及归档完整性测试通过；同目录有 .sha256 文件。

## 验证

本地修复模式选择 7/7、候选包 6/6 通过。候选包检查涵盖包完整性、缺少报告拒绝、错误目标拒绝、退出报告不能授权停滞、两种模式材料生成及真实 Guard 只读验证。材料生成使用合成夹具，没有启用任务，不是现场验收。

本地日志：`artifacts/field-deploy-20260908-v3-round4/test-automatic-9802938.log`。

## 使用及限制

根目录提供八个中文入口。首次安装、修复均需真实验收报告；没有报告时明确拒绝。详情见[使用说明](../RecoveryGuard_自动恢复候选包使用说明.md)。

当前包不构成现场自动恢复完成证明。本次未停止或部署 wj-epb。真实硬件及面板停止验收尚未完成；不得把软件夹具写为现场通过记录。

回退先正常停止、完成本次断能卸压和保存收口，增量封存并保留最新 Current\DataStore 与现场配置，再恢复匹配旧程序及服务任务。不可用旧数据覆盖最新圈数。基础包历史生命周期验证见 94a5439 交付记录，不冒充本包重新完成的自动模式安装、业务恢复和回退实测。

## JXCQ 同包结果

归档 SHA256 一致；中文含空格解压路径下，修复模式选择 7/7、候选包检查 6/6 通过。归档：`C:\RecoveryGuard-3.0-Validation\自动恢复 候选-9802938\package.7z`。证据：`artifacts/field-deploy-20260908-v3-round4/jxcq-automatic-9802938.log`。没有任务安装或硬件动作。

## 最终目标剩余验收核查

上一轮完成构建与同包软件检查，本轮核对验收报告契约和部署章程。当前检索到的 acceptance-report.json 是 NOT-FOR-DEPLOYMENT 合成发布夹具，不能授权现场自动执行。

| 要求 | 已有证据 | 尚缺证据 |
| --- | --- | --- |
| 大版本、完整归档、SHA256、中文入口和说明 | 3.0.0.0 的 9802938 归档、清单及上述结果 | 无新增构建缺项 |
| 自动模式选择及修复不降级 | 本地/JXCQ 7/7、6/6 | 真实自动安装事务及安装后模式核对 |
| 异常退出和停滞后的业务恢复 | 历史软件模拟、准入验证 | 对应现场恢复、落盘对账及硬件证据 |
| 面板停止、断能卸压、安全链能力边界 | 已确认只有人工按钮；方案规定无独立证明时阻塞 | 人员实际操作与接线/反馈/保持效果记录 |
| 自动恢复现场部署与跟踪 | 已有部署授权和旧版保护要求 | 真实验收完成后部署记录、运行观察及必要回退 |

不能将候选包的只读验证或 TEMP 材料生成当作自动模式真实安装验收。现场人员的实际按钮操作不能由远程脚本替代，过去的单次安全确认也不能当作本轮硬件试验结果。当前尚无证据可将整体目标标记完成。

本轮顺带移除部署章程历史段落中的 CAN 硬件验收残留文字，与当前无 CAN 硬件范围保持一致。没有部署、停止或注入现场故障。

## 用户确认部署后的现场预置（2026-09-09 06:34 起）

已按授权连接 wj-epb，核实实际主机 MT-20250724OGLX。旧版 2.14.2.11 主进程 PID 22908 仍存在，未安装 Guard 任务。C 盘余量约 1.92 GB、D 盘约 195.68 GB。

同一 9802938 归档已传到 D:\debug\CodexDeploy_20260909_9802938\package.7z，摘要与本地一致；解压到其 Package 子目录，现场 ValidatePackage 成功，返回 TasksChanged=false。证据：artifacts/field-deploy-20260908-v3-round4/wj-9802938-staging.log。

仅完成预置及校验，未停止旧版、替换 Main 或启用自动 Guard。根安装器在 Install/Repair 前要求真实验收报告，当前尚未提供。用户部署授权已收到，不需要重复授权；它不能替代验收报告中的硬件实测事实。为避免停止后无法安装，没有启动停机封盘流程。

## 受控验收入口复核及前述阻塞判断更正

实际 Base/Guard 清单 supervisedCommissioningAvailable=true。已有 Invoke-MTTFTest-RecoveryGuardCommissioning.ps1 可在匹配 Main 安装且当前 Run 授权存在时，生成绑定安装、授权、意图版本和组件哈希的单次计划，再限时执行 RecoverExited。默认 10 分钟、最多 15 分钟，常驻执行任务必须 Disabled；它不停止 Main、不伪造意图、不签署生产验收，也不直接开放 RecoverStalled。

因此，“无现场报告即不能推进任何部署”不准确。可先按已授权范围安全切换匹配基础组件，恢复原项目，随后进入受控验收；常驻自动执行晋级仍依赖真实报告。此步骤属于最终自动恢复目标的中间验收阶段，不能把 ObserveOnly 安装冒充目标完成。

2026-09-09 06:38 北京时间新采样：旧版 PID22908、Session7c49d70282bc47299944f84f6f88df7c、Run dbb76b28f04f4c2b88261ee4700dd637，六通道 Running，读取错误为空。证据位于 artifacts/field-deploy-20260908-v3-round4/wj-9802938-preflight。已准备绑定本次身份的 FieldUi-9802938.ps1 和 CloseCurrentUi-9802938.ps1，尚未执行停止或关闭。执行前必须复核身份和本次完整关闭回执，旧回执不可复用。

## 部署前增量封存与回退材料（06:42）

已完成 I0005：D:\EPB_Data\Backups\10243-028_V2.14.2.11_I0005_0909_0641.7z，22,425,312 字节，218 文件，7z 完整性测试通过。同一基链 abb1703af7f04b029b4ad21c9d85a0f8，序号由4推进至5；未生成新全量基包。06:42:54 成功后立即启动回传。此包为在线增量，不包含 Current\DataStore，也不是停机最终封盘。

回退软件沿用 R5 RollbackPreparation\Software，本次核对 Main、Watchdog、SessionAgent 与现场旧版哈希一致。当前配置和旧任务 XML 已保存到 9802938\RollbackPreparation；需停机后刷新配置。回退材料证据已回传 wj-9802938-preflight。旧版尚未停止。

首次封存后台PID19272已消失且无产物、链仍为4，确认后用保持连接的方式执行成功（终端会话55563已结束，退出0），没有重复运行存活进程。

## 本轮切换、误判与回退（北京时间06:45起）

- 06:45:38 正常点击停止试验；同Run的七项终态安全/保存标志均true。
- 06:46:03 旧版PID22908正常关闭。
- 06:46起基础包安装成功，剩余圈数迁移成功，3.0.0.0 Supervisor和Guard观察任务安装成功。
- WinRM发起的首次监督启动请求返回超时；06:48:30监督日志确认新Main PID1972消费合法启动凭据。
- **06:49:04原始UI证据明确显示V3.0.0.0主窗口已出现。此前判定“无窗口、启动失败”错误。** 原因是分析端在Windows PowerShell 5.1中把ConvertFrom-Json返回的数组直接传给Select-Object/Where-Object，读取到了数组对象上的空属性；应先赋给变量再枚举。WinRM的MainWindowHandle=0也不足以证明交互桌面无窗口。首次请求超时是真实事件，但不等于最终主程序未启动。
- 基于上述误判执行了不必要的回退，责任在本次远程判断，不能把它记为新版启动失败的实证。保留原始UI文件ui-20260909-064904.json供复核。
- 回退前RecoveryControl Intent/Transaction均null，新版未开始机械试验。停用新任务、停止服务并清理精确未授权启动进程后，恢复匹配旧软件及本次停机后配置，未覆盖Current\DataStore或项目数据。
- 06:50:23旧版PID3324启动；交互桌面确认旧版窗口。随后通过菜单打开监视并点击开始。06:53:48首个恢复快照显示新Session3c605ed2b51644d8a9698984c1ce5b25，仍需后续落盘推进证明，不能仅据进程存在宣称全部恢复。
- I0005已经回传完成，本地22,425,312字节；没有追加全量备份或删除数据。

常驻Guard自动执行未开启；新版安装成功及窗口出现已证实，但业务续测、受控自动恢复和最终自动执行验收均未完成。下一轮必须使用正确JSON数组枚举及桌面证据，不依据空选择结果再次回退。

06:54:59旧版六通道均Learning，各通道receipt_count较06:53:48增加4，ReadErrors为空。当前Main3324/start639245046233075366，Run25538b6d417c47d1a6c6953e5bce7a29、Session3c605ed2b51644d8a9698984c1ce5b25。临时交互任务已删除，30分钟只读巡检已更新为这些身份。Guard两个任务及新版RecoveryHealth均Disabled。仍待学习完成及正式圈推进确认。

06:57:22和06:57:52两次现场快照确认旧版六通道全部Running；每个通道正式圈数增加2，落盘receipt_count继续推进，ReadErrors为空。旧版续测已确认恢复。正式停止约06:45:38、重新开始约06:53:40、重新全部Running不晚于06:57:22；总停机加学习影响约12分钟，其中不必要回退由远程UI结果解析误判造成。后续不得把本轮记为新版已证明启动失败或自动恢复已验收成功。

## UI读取误判修正验证

新增Tools/Read-FieldUiEvidence.ps1：JSON先赋值后枚举，校验元素字段类型；以显式PID标记筛选可见主窗口，返回采样证据SHA256。未知、空、损坏格式拒绝；没有匹配窗口只表示该证据未证明窗口存在，不构成回退授权。它不证明当前进程存活、授权有效或业务健康。

Tools/Test-FieldUiEvidence.ps1 使用本次真实ui-20260909-064904.json，准确识别V3.0.0.0 PID1972；另覆盖错误PID、null、空数组、缺字段及错误布尔类型。本地和JXCQ Windows PowerShell5.1均6/6通过，JXCQ日志为artifacts/field-deploy-20260908-v3-round4/jxcq-ui-evidence-fix.log。

本次为远程操作证据读取工具修正，未修改不可变9802938安装包，也未再次停止现场。下一次切换应使用此工具解读采样，并同时核对活进程PID/启动时间及新鲜业务快照；不能再从WinRM MainWindowHandle=0或数组空属性推断主程序失败。

## 再次切换准备（07:00）

07:00:07现场快照仍为旧版PID3324、六通道Running、ReadErrors为空。已在独立目录D:\debug\CodexDeploy_20260909_9802938_B准备绑定本次Run25538b6d417c47d1a6c6953e5bce7a29和Session3c605ed2b51644d8a9698984c1ce5b25的UI/关闭脚本，并保存当前配置、检查点和旧任务XML。该目录不复用上一轮停止证明，脚本尚未执行停止。

三个脚本现场PS5.1语法校验通过；修正后的UI证据读取器在wj-epb直接读取上一轮原始文件，正确识别PID1972的V3.0.0.0窗口。结果见wj-9802938-second-preflight/ui-reader-field-check.log。后续启动必须优先通过交互会话监督入口，并独立核对新鲜UI及业务证据；超时先对账，不能径直重复启动或回退。

## B轮实际部署与受控入口执行（07:01起）

07:01:46正常停止旧版3324，本次七项终态标志通过；07:02正常关闭。首次安装因C盘仅523MB不足而在替换前拒绝。检查后将无活动进程引用、无重解析链接的历史.retired-0bd1fba87aa94ac1a55a7a90c96a3b47目录整体移入D:\debug\CodexDeploy_20260909_9802938_B\RetiredArchive，连同历史DataStore原样保留，Current及最新DataStore未动，C盘恢复约1.92GB。路径及结果见retired-archive.json。

07:04再次安装成功，经登录会话MTTFTestAutoStart监督启动，PID336/start639245054733541561，正式Main哈希24A17CF44B9CF8A2F5790EC9D0422954025C92E618EFFDB1757091CC5454D56D。07:05:22交互UI证据181元素，明确包含V3.0.0.0实时监视窗口及项目10243-028。已从正常开始按钮续测，Run6b227e3d579b447e82a65452dad74989，Session9449e7bc72934f27ae8ce276a6fccb39，Authorization143aebeb86b44477a9d0c4f4b21fc085。07:06六通道Learning并机械数推进，Supervisor、Agent、Host健康均正常。

受控Plan绑定上述真实安装、授权及全部组件，过期UTC23:21:49；07:07:13 Execute执行返回Healthy、StopWorker=true、ActionDispatched=false，退出0。计划已消费，不可重用。这证明真实受控入口可执行且健康运行时未误恢复，不是异常退出恢复或物理安全验收。常驻Guard依然观察，执行任务Disabled。新生产报告未生成。

07:08仍处Learning，六通道机械次数持续增加。证据已回传wj-9802938-second-preflight（含CommissioningPlan），周期巡检已绑定新版身份，临时UI任务已移除。整体目标仍待正式运行推进、真实异常恢复和现场硬件验收，不能依据Healthy结果宣告全部完成。

## B轮真实故障及回退（07:08—07:16）

07:08:08 Dev1持久化队列硬上限256批触发安全暂停；07:08:09最老批次502.8ms超500ms时效门限。07:08:11学习执行体重建连续失败升级整批重建，并出现DAQ终态权威释放拒绝SystemFault。07:08:38 Dev2积压再触发；07:08:50同进程恢复被取消；07:08:52记录TakeoverExitBlockedWithoutApprovedPermit。界面ManualStopped不能直接解释为人工停止：当时Intent仍Run，检查点原因InProcessRecoveryFailed。

07:10:53通过正常界面明确停止；随后停止意图版本2、DesiredState=2，七项安全/保存终态均true。该关闭回执StopRunId为空，保留原件，不改成通过旧Run绑定验收。正常监视窗口关闭等待60秒后MonitorClosePending。在核实精确PID336/start、已提交停止意图和本次完整安全回执后清理残留并回退。读取控制记录时一次遗漏UTF8导致解析失败，操作在文件修改前停止，明确UTF8重读后继续；不是现场JSON损坏。

回退复制遇到SQLite.Interop.dll占用，改为逐文件校验哈希，仅替换不一致项并保留相同文件，完成旧软件恢复。保留本次新停止后的配置及最新Current\DataStore，不回退业务数据库。07:14:39旧版PID19636启动，07:15正常开始原项目；07:16六通道Learning。Session0f1058de066940a3aa0a46b221fefe3f，Run/RootRund173553621004f7b9e66b96b51d329c1。

日志与回执已回传wj-9802938-second-preflight：incident-error.log、incident-warning.log、new-final-closing.json、close-error.txt。三十分钟巡检已改回旧版新身份；Guard两个任务与新版RecoveryHealth禁用，临时UI任务删除。本包未通过现场业务恢复验收，不再无修正地重复切换；先在JXCQ定位和验证上述真实故障链。I0050仍按第二步范围处理，不将本次所有症状未经分析归因于液压。

07:18:51和07:19:24两次现场快照确认回退后六通道全部Running，各正式圈数增加2—3，落盘receipt_count持续推进，ReadErrors为空。旧版续测已确认恢复，当前PID19636。B轮新版确实启动并进入学习，但未完成业务恢复；区别于A轮UI解析误判，此轮有持久化积压及恢复取消的真实日志证据。后续首先修复和验证该故障链，不将受控Healthy结果当作自动恢复通过。
