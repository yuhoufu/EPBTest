# V2.17.2.0：I0046 修复实现与交付记录

日期：2026-09-06。实现基线：fb498ba（仓库已有版本 2.17.1.1）；现场封存为 2.17.1.0。新版本统一为 **2.17.2.0**。

## 1. 本轮结论与范围

按用户最新要求，三项 DAQ 存活参数及其独立监督逻辑恢复至 2.14.2.11（6ed4d47）。这项明确要求覆盖此前分析文档中“暂保持 75/100/250”的建议。其余按[封存证据复核与版本对齐建议](2026-09-06_V2.17.1.0_I0046_封存证据复核与版本对齐建议_Codex.md)实施：修退出交接、安全截止顺序、相关容量故障归并，并补充写盘阶段诊断和保持耐久语义的优化。

本轮交付代码与可安装的软件包；没有在本机执行现场设备上电、CAN/NI/液压操作、现场覆盖安装或长时间带载验证。软件回归通过不等同于已复现并消除现场所有长尾停顿。

## 2. 参数及逻辑如何对齐

用户消息中的 DagLiveness 实际配置键为 **DaqLiveness**。

| 项目 | 本次恢复结果 |
|---|---|
| DaqLivenessWarnThresholdMs | 250ms；读取范围 100～5000ms |
| DaqLivenessSuspectThresholdMs | 1500ms；读取范围 200～30000ms |
| DaqLivenessTripThresholdMs | 5000ms；读取范围 300～300000ms |
| 独立存活监督年龄 | 仅 CallbackAgeMs，恢复旧版判定 |
| Trip 确认 | 连续 3 次监督确认 |
| 检查周期 | 保持 20ms |
| 历史空窗已恢复 | 保持旧版 RecoveredGap，不事后因已恢复空窗重建 DAQ |
| 监督适用条件 | 保持旧版运行、带电/升级观察、恢复状态限制 |

`Controller/EpbManager.DaqLiveness.cs` 恢复为旧提交对应文件；`EpbManager.cs` 恢复默认值和范围；`MTTfTest/App.config` 改为 250/1500/5000。

这意味着从 5000ms 起连续三次检查确认，不能解释成等待 15000ms。Warn 仍用于执行器新鲜度截止的参数传入，其实际消费方式保持现有调用关系。独立控制数据质量、100ms ControlHardFaultAge、代次校验、OFF 未确认 5s 接管、持久化有界队列与超时并未撤销。样本陈旧但回调新鲜时，旧版存活监督不 Trip，独立控制质量仍拒绝陈旧样本。

安装后必须检查安装目录实际 `MTTFTest.exe.config`。如果现场配置被部署规则或人工修改保留，以实际加载值为准，不能仅检查源仓库。

## 3. P0：主子窗体共享正确的退出类型

现场确定的缺陷是：子窗体授权先按 SessionClose 关闭 sidecar，之后主窗体的替换退出复用已经终结的共享关闭任务，造成批准许可后被普通 ApplicationClosing 取消。

本次修改：

1. `Main_Frm` 将本次退出类型传到所有监控子窗体。
2. 子窗体授权将该类型继续传入主窗体共享关闭入口。
3. 普通 SessionClose 遇到已经确定的看门狗退出所有者时，沿用所有者的类型；显式 ApplicationExit 保留人工退出语义。
4. sidecar 收到晚到普通 ApplicationClosing 时，只有退出回执、关闭墓碑、当前进程 PID/启动时间及当前许可绑定都匹配，才保留接管。人工停止、会话撤权、失效许可仍优先阻止接管。
5. 保留路径记录 `LegacyApplicationClosingPreservedReplacement`，继续观察既有退出事务。该分支不会凭空生成许可，也不跳过旧进程退出证明和实际拉起流程。

关键文件：`MTTfTest/Main_Frm.WatchdogUi.cs`、`MTTfTest/FrmEpbMainMonitor.cs`、`Watchdog.Protocol/WatchdogSafetyReceipts.cs`、`MTTFTest.Watchdog/WatchdogHost.cs`。

## 4. P1：安全投递先于同步取消和观察者

DAQ 恢复的第一段采用 `HighPrecisionTimer.RequestSafetyPauseNonBlocking` 封住下一圈，避免 `Timer.Pause` 同步发布外部观察者。随后冻结原圈身份和持久化边界、提交整组最高优先级 OFF、启动共享电源 Disable 及拒绝项兜底、撤销液压参与权并投递释放。

周期 CTS 从字典摘除时捕获确切旧对象，再单独调度 Cancel/Dispose。取消回调不占用截止执行线程；迟到任务不会再次按通道查询并取消新一圈 CTS。边界冻结仍保留现有身份锁定规则，未通过放宽边界消除等待。

新增 `FieldMetric DAQ_CUTOFF_TIMING`：WatchdogsMs、PauseAdmissionMs、FreezeBoundaryMs、SubmitOffMs、StartPowerDisableMs、OffFallbacksMs、HydraulicReleaseMs、CancelCyclesMs、PublishObserversMs，携带 Device、RunId、RunEpoch、RecoveryEpoch、CorrelationId。计时用单调时钟；日志在安全投递阶段结束后消费。

物理 OFF 未确认仍受独立短预算监督，不因 RecoveryActive 豁免。阶段计时在调用返回/抛异常后汇总；永久卡在某阶段时仍需依靠已有监督、转储及线程栈，不能指望该阶段提前产出完成耗时。

## 5. P2：写盘诊断与确定可保持语义的优化

### 5.1 分段诊断

新增 `EpbWriteTiming`，通过生产监控窗体连接到采集器已有内存诊断环，不在写盘锁中同步写日志文件：

- 每设备批次记录总时长、Device/Generation/Sequence、样本数和成功状态。
- 超过 50ms 或失败时，Detail 记录线程 ID、通道锁等待、raw gate 等待、raw 准备到提交总时长、raw Commit、索引锁等待/Commit、ring/MMF Flush、checkpoint 和 raw 回收。
- 普通批次保留数值总耗时与刷盘/提交数值，避免逐批格式化长字符串。
- CompleteCycle、AbortCycle、ExportCycleAttempt 记录操作总时长、通道/圈号、成功状态；这些操作不具备设备批次的 Sequence 身份。
- 诊断 Kind 为 `StorageWrite`，与已有 `Persistence` 队列消费总耗时区别使用。阶段存在包含关系，不能把 RawStage、Checkpoint 与其子阶段相加当成总耗时。
- 诊断回调在本次方法持有的写盘锁释放后执行；回调异常不能改变写入成功或失败的结果。外层非本方法持有的锁不在该保证范围内。

诊断覆盖进入设备写入核心及圈关闭/尝试导出；入参校验前的失败仍由已有非法批次隔离证据描述。磁盘卷 IOPS、过滤驱动与 OS/GC 调度不由这些软件阶段计时直接测得，后续现场用 ETW/磁盘性能采集与时间戳关联。

### 5.2 本次优化

- 原始日志预检沿用原有活跃圈、序列、截止、容量条件；无待写帧时跳过空事务及共享 raw gate。
- checkpoint 对同一圈的多帧只更新一次最大样本前缀，减少重复 SQLite 命令和索引 UPDATE；仍检查每个圈的索引身份存在，最大值用 checked 计算。
- 保持 FULL、先 raw 持久化、再 ring flush(true)、再索引提交、最后回收 raw 的顺序。Persisted/FIFO/重试幂等/异常圈终态规则不变。

本轮没有启用文档中要求先评估的 group commit，也没有将通道状态锁移出耐久边界；这些方案需要额外的所有权设计和掉电测试。没有降低 synchronous，也没有添加 Defender 排除。不能把上述优化描述成已证明修复了现场磁盘饱和：封存只证实存在慢写，未证实正常期持续饱和。

## 6. P3：容量事件由当前恢复收口

`DaqPersistenceQueueFull` 首次出现仍走统一安全截止/恢复。未出现 DurabilityBlocked 的容量事件，不再在 Begin 后无条件另开升级链；相同 Run 的既有设备恢复和 CorrelationId 归并机制继续使用。

实际写失败、DurabilityBlocked、恢复超时和真实数据缺口仍升级；物理 OFF 确认与恢复无进展预算仍有效。队列容量、暂停/恢复水位及数据守恒逻辑保持原值。

## 7. 验证与安装交付

新增 `I0046AlignmentTests`，覆盖退出类型、人工停止/撤权/PID复用/租约/nonce/事务不匹配、取消回调阻塞下的安全投递次序、容量故障与真实耐久故障的区分。

写盘新增真实 SQLite/ring 回归：诊断异常不能篡改写入结果、重复序列不提交原始事务、失败批次状态正确、多帧 checkpoint 后索引与导出仍有完整 101 样本。旧版 liveness 用例恢复三次确认；保留陈旧控制样本拒绝测试。

正式产物从独立干净 Git 快照构建，完整打包主程序、匹配版本的 sidecar/协议/SessionAgent/SafetyAgent 及一键安装脚本。原工作目录和用户 GLM 分析保留。具体构建提交、测试结果、包路径、SHA256 见下方交付追加记录。

### 现场验收仍需执行

1. 在隔离的工程数据副本验证安装配置为 250/1500/5000，确认 EXE 与全部看门狗组件版本一致。
2. 同机同工程同通道比较首次保护、有效圈和恢复成功率；不要仅比较窗口存活时长。
3. 分别注入短时慢写与永久无进展，核对 OFF 提交、当代 OFF 确认、原圈作废、单一恢复所有者和有界升级。
4. 真实接管验收应看到旧 PID 退出、唯一新 PID、许可消费及恢复提交；仅有 Approved/墓碑匹配不算替换完成。
5. 重放、断电恢复和六通道带载长稳必须在现场安全试验条件下验收。本轮本机测试不能替代这些结果。

## 8. 本机执行结果

构建源码提交：`1185a09d62e84b1982c648168b371118d17b9ce4`，分支 `codex/i0046-v2.17.2.0`。独立源码工作树位于 `artifacts/i0046-release-source`；根工作树保留相同代码修改，未切换用户当前分支。本节为构建后的交付记录，不属于该源码提交内的初始文档。

| 验证 | 实际结果 | 日志 |
|---|---|---|
| Debug x86 解决方案编译 | 通过 | [编译日志](../../artifacts/i0046-debug-build.log) |
| Release Any CPU 解决方案编译（主程序 x86） | 0 错误，45 条警告 | [正式构建日志](../../artifacts/i0046-release-build-final.log) |
| Release 控制全套 | 753/753 | 同上 |
| Release 写盘全套 | 66/66 | 同上 |
| Release 电源 .NET 8 测试 | 19/19 | 同上 |
| 真实 WinForms UI 专项（Debug） | 29/29 | [UI 日志](../../artifacts/i0046-winforms-tests.log) |
| 真实 Runtime/sidecar 接管保留专项（Debug） | 8/8 | [接管日志](../../artifacts/i0046-sidecar-takeover-tests.log) |
| 真实 Runtime/sidecar 人工撤权专项（Debug） | 1/1 | [人工退出日志](../../artifacts/i0046-sidecar-operator-tests.log) |
| I0046 新增策略和截止次序回归 | 4 项通过，同时纳入控制全套 | [专项日志](../../artifacts/i0046-targeted-tests.log) |
| 旧版存活源码、默认值、范围核对 | 精确一致 | [核对 JSON](../../artifacts/i0046-liveness-alignment.json) |

真实 sidecar 专项验证的是已有墓碑写失败路径的保留/撤权语义；UI 专项验证生产窗体与共享生命周期接口。本轮没有把它们合称为“现场 I0046 全流程已复现并完成新 PID 恢复”：旧 PID 退出到新 PID 及恢复提交的整条带硬件链仍需现场验收。

主程序、Controller、Watchdog、SessionAgent、SafetyAgent、协议/客户端等采用统一版本 2.17.2.0。DataOperation、IO.NI 等历史固定版本程序集仍保持其既有 1.0.0.0 标识，以正式包 SHA256 清单确认本次具体文件，不能只看其 FileVersion 判断是否已更新。Release 配置实测为 250/1500/5000。

首次干净工作树发布流程在隔离部署测试处发现其固定读取 Debug 产物，而该工作树尚未构建 Debug；此前 Release 编译及三套测试已通过。补齐同一提交的 Debug x86 构建后，隔离部署测试单独通过 28/28，再完整重跑正式发布流程，未修改或绕过测试。该测试只操作临时目录，没有修改本机服务或硬件。见[部署回归日志](../../artifacts/i0046-deployment-tests.log)。

最终完整发布流程通过，现场验收脚本自动测试 **49/49**，隔离部署 **28/28**，部署契约、命令行解析等门禁均通过。注意 49 项是验收脚本自身的自动测试，不是现场实机验收结果。

## 9. 最终安装包

- [一键安装 7z 包](../../artifacts/deploy/V2.17.2.0_正式版_1185a09d62e8_QUICKDEPLOY_R29.7z)
- [压缩包 SHA256 文件](../../artifacts/deploy/V2.17.2.0_正式版_1185a09d62e8_QUICKDEPLOY_R29.7z.sha256)
- [已解压的一键安装入口](../../artifacts/deploy/V2.17.2.0_正式版_1185a09d62e8_QUICKDEPLOY_R29/一键安装正式版.cmd)
- [原始正式包构建身份](../../artifacts/releases/V2.17.2.0-1185a09d62e8-20260906_094716/build-identity.json)
- [独立清单复核结果](../../artifacts/i0046-delivery-verification.json)；[打包日志](../../artifacts/i0046-bundle.log)

压缩包 **34,202,515 字节（约 32.62 MiB）**，SHA256：

```text
77bc4d8f7cba552e7a6c24e1100910106a3286e45aa6bbbb93238e62c6c82fbb
```

7-Zip 完整性测试通过；外层部署清单复核 133 个文件、内层程序清单复核 117 个文件，均与 SHA256 一致。包身份为 `FORMAL_RELEASE`、`gitDirty=false`，程序包与部署工具来源均为 `1185a09d62e84b1982c648168b371118d17b9ce4`。当前工作目录的 26 个代码/工具文件与该提交一致，见[源码快照核对](../../artifacts/i0046-source-snapshot-check.json)。

将整个 7z 解压到现场本地目录，保持外层脚本与 `Package` 子目录完整，再运行“一键安装正式版.cmd”。默认程序槽为 `%ProgramFiles(x86)%\MTTFTest\Current`，业务 XML 配置在 `%ProgramData%\MTTFTest\Config`。安装后复核 `Current\MTTFTest.exe.config` 中的三项参数。安装只部署程序，不自动开始机械试验。

包身份保留 `fieldValidation=PENDING_USER_HARDWARE_AND_168H`：软件发布流程已经完成，现场硬件与 168 小时长稳仍待验收。本次未在当前电脑执行现场覆盖安装。

复现构建时应先在干净工作树完成 Debug x86 构建，再执行 `Tools/Build-Release.ps1`，最后将它输出的正式目录传给 `Tools/New-QuickDeployBundle.ps1 -ReleaseDirectory <正式目录>`。本轮使用该完整流程，未通过跳过测试生成正式身份。
