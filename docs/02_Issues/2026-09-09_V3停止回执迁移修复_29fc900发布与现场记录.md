# V3停止回执迁移修复：29fc900

完整候选包已生成，JXCQ同包软件生命周期验证通过，但R6现场迁移预检发现正常关闭时间序列被误拒。此包暂停用于现场安装；未替换现场软件，已恢复旧版续测。Guard自动执行尚未验收或开启。

## 安装包

路径：`D:\Github\wanxiang\EPBTest\artifacts\deploy\V3.0.0.0_正式版_29fc9002a535_QUICKDEPLOY_GUARD.7z`。

SHA256：`F895C7BF99518EA4EC459ED494F40E0B5493109912A6440D5A43FBCB9C0B3FBB`。

34,323,560字节，143文件，7-Zip最大压缩率及解压完整性检查通过。发布提交 `29fc9002a535c767c3daa31a34d6a842c4b752f4`，构建来源为干净的独立发布工作区。根目录保留八个中文CMD入口，主程序位于Package，独立Guard位于Guard；安装操作须在解压后执行。包内提供SHA256、组件清单、中文说明和回退说明。

| 组件 | SHA256 |
| --- | --- |
| MTTFTest.exe | 84A8A414772DE73F9173BA64A587F1C537ABF8124FDDC539E1112412492CCC7C |
| MTTFTest.RecoveryGuard.exe | BF9CC4D450044DFFC155903A746C338418059820077556C0EB015522F81AC9DA |
| MTTFTest.RecoveryControl.dll | CA24E7FC3359CDC5C15B8737123B9B8EB47D02496CAC4F2225048BED61ED8A7D |

## 修复及验证

旧2.14.2.11正常停止时，完整终态回执已经证明断能、卸压、持久化收口，但旧检查点三项标志仍false，原安装器因此拒绝迁移。本次加入当前项目实时回执校验：绑定Run、Session、代次、旧EXE摘要和进程身份，要求人工停止、七项安全结果完整、重新拉起被禁止；单独检查主程序已退出。维护互斥及启动任务停止后才执行，不修改旧标志、不迁移旧授权。最后有效活动观察超过10分钟拒绝迁移，不能通过一般会话更新时间续期；这不改变自动恢复60分钟断电过期规则。

JXCQ对历史隔离夹具完成23项校验和7项实时文件/安装迁移测试。发布控制803/803、持久化74/74、电源19/19、现场门禁49/49、最终Guard97/97通过；重复失败验证300次、3589步、模拟时间215520秒。模拟时间不代表实际连续运行时长。

最终同包测试目录：`C:\RecoveryGuard-3.0-Validation\批次修复 验证-29fc900\正式 安装包`，包含中文和空格。八入口均退出码0；02:31监督启动Main PID23364，显示V3.0.0.0，正常关闭成功；卸载后恢复测试前2.17.3.0，主程序及8份配置摘要一致，MainCount=0，Guard任务为空，临时交互任务已删除。CMD通过管理员WinRM和交互桌面任务执行，不冒充人工双击/UAC点击或硬件试验。

本地证据目录：`artifacts/field-deploy-20260908-v3-round4/jxcq-29fc900/`；构建日志 `build-final-29fc900.log`；最终Guard测试 `test-final-29fc900-guard.log`。

## 现场限制与后续

wj-epb预置路径 `D:\debug\CodexDeploy_20260909_V3_R6\package.7z`，实测SHA256与上述一致。预置不等于安装。现场旧版PID21176继续运行，最近02:19只读检查仍正常。

后续切换使用本次新鲜停止证据，增量封存沿用既有链，不重复全盘封装。最新Current\DataStore、项目数据库和圈数必须保留，不能用旧备份覆盖。切换失败按匹配版本恢复旧程序，并验证业务与持久化推进后才宣告回退成功。

真实恢复、物理安全及人工面板停机优先验收尚未完成。面板只能人工按下，已询问现场配合时间；不预填验收通过。当前包仍ObserveOnlyCommissioning，Execution Disabled。当前无CAN硬件，不检测CAN。I0050液压恢复状态发布一致性修复仍为第二步。

## R6预检误拒与恢复旧版

02:36:54正常停止，02:37:01.690取得七项完整安全终态；02:37:30.607主程序正常退出。退出时旧检查点以 `LastReason=MonitorClosing` 更新至02:37:30.365，合法地晚于安全终态。29fc900要求检查点不晚于安全终态，因此返回 `LegacyStopEvidenceExpiredOrClockInvalid`。这是本次迁移校验过严和测试夹具遗漏正常关闭后更新造成的误拒，不是新的DAQ或硬件故障；安装器尚未替换程序。

02:39:06重新打开旧版PID22908，启动UTC ticks639244895462899377，02:39:59点击开始；02:41六通道Learning且数据推进，ReadErrors为空。Session `7c49d70282bc47299944f84f6f88df7c`，Run/RootRun `dbb76b28f04f4c2b88261ee4700dd637`。实际停止到再次开始约3分钟，之后的学习阶段不计为已恢复正式运行，须继续对账。原数据、配置和目标未被备份覆盖，巡检已更新身份并恢复。

修复中的源代码只允许 `MonitorClosing` 检查点晚于终态且不晚于当前时间，仍要求完整安全回执、相同Run/Session/进程身份、无主程序和无新活动；其他原因的后续检查点、过期及未来时间仍拒绝。已加入本次真实时序夹具；此修复尚未包含在29fc900压缩包中。R6证据位于 `artifacts/field-deploy-20260908-v3-round4/wj-r6-preflight/`。同链增量封存已开始，不重新全量打包。

后续确认：02:44:40和02:46:15六通道全部Running，各通道正式圈数和SQLite receipt_count均增加，ReadErrors为空。`r6-resume-verified.json`保存对账结论。临时CodexR6FieldUi任务已删除，30分钟巡检已恢复并绑定PID22908及新Run。I0004增量包 `D:\EPB_Data\Backups\10243-028_V2.14.2.11_I0004_0909_0240.7z`，24,939,916字节、402文件，完整性检查通过；02:45已回传本机同路径，Robocopy正常复制结果为1。此增量为续测后的在线快照，不冒充停止后不再变化的最终封盘。

正常关闭时间序列修复提交为 `94a5439b0d8f1bf829cca4679aede24812e19481`。本地旧夹具及JXCQ本次R6夹具25/25证据校验、7/7迁移集成测试通过。新完整包正在构建（`build-final-94a5439.log`）；构建完成前不对外宣称已有新最终安装包，不在已校验压缩包内临时修改脚本。
