# EPB 3.0 与独立 Guard：新任务交接（2026-09-09）

## 交接目的与执行边界

用户明确要求把本对话落地交接文档，把目标交给新对话执行。原任务 ID：01a07aac-87eb-73d2-b699-cd2ec70e5626。本文件是执行摘要，不是完整逐字聊天记录；历史记录通过本文列出的文档和证据追溯。本文中的现场状态均有时间边界，接手后先只读核实，禁止当作实时状态。

原目标原文：按实施方案，实现；迭代大版本号；最终生成安装包；落地文档。

具体目标：完善3.0.0.0 Main及独立部署的RecoveryGuard，解决新版启动/学习/持久化/恢复链问题，形成可审计、可一键安装和回退的完整7z包，先在JXCQ隔离验证，再在获得新的部署确认后到wj-epb验收真实硬件自学习、正式运行和安全自恢复，完成Markdown交付。

目标未全部完成。原目标当前为blocked，原因是现场新版切换等待用户确认；不得将其改为complete来模拟迁移。可在新任务按用户本次明确移交目标的授权建立接续目标，不设置token预算。旧目标保留历史状态，旧任务不再执行该目标。

### 最新用户意图优先于旧文档

1. 新版部署：用户最新明确“等我确认后再部署，wj-epb”。此后仅确认了旧版重启，未授权部署5d2eb9b新包。旧文档“自行部署/无需再次确认”不能覆盖这条新限制。
2. 现场操作：09:13出现人工停止后，用户回复“可能是我误触了，我来重启”。当前由用户重启，Codex只读观察，不再次点击启动/停止，不覆盖人工停止意图。
3. 数据只做增量，沿用既有基包链，不传StartNewBase、不重做全盘封装；不得覆盖Current\DataStore、项目数据、检查点或试验计数。
4. 当前没有任何CAN硬件。不检查CAN设备/驱动/连接，不设置CAN准入条件。遗留代码不改变范围。
5. I0050液压恢复状态发布一致性修复仍为第二步；不能在第一步状态中声称已修复。
6. 人工停止优先，全部卡钳完成不续测；单个永久故障隔离通道沿旧逻辑；冷却后继续尝试；断电超过最后可信监督记录60分钟视为过期，不自动处理。面板按钮只能人工按下，不能冒充可远程独立断能能力。
7. 用户历史安全确认不能自动证明新事故安全；不得伪造安全标志、恢复许可、验收报告，不能绕过NI/电源/液压实际安全门禁。现场硬件验收与JXCQ模拟严格分开。

## 工作区与发布源码

原目录：D:\Github\wanxiang\EPBTest。主工作区大量已修改和未跟踪文件，包含整个Guard实现与本轮文档，不能git reset/clean或全盘覆盖，不能只看主分支HEAD就认定候选版本。

已核实干净的发布源码工作树：
D:\Github\wanxiang\EPBTest\artifacts\ReleaseValidation-20260908\source

- 分支：codex/release-v3.0.0.0-20260908。
- HEAD：5d2eb9b273099775d5b19c9d3dfd6d4232b80b1e。
- 本次交接再次检查git status为空。
- 新建任务默认worktree可能从项目默认分支创建，不保证含上述源码；先核对HEAD和差异，再基于明确候选提交开展代码工作。原工作区未提交资料从本文绝对路径读取；不要假设新工作树已自动带入。
- 主工作区修改清单保存在相邻交接证据目录中。不要全盘提交，不提交凭据、日志或构建产物。

MSBuild：C:\Program Files\Microsoft Visual Studio\2022\Professional\MSBuild\Current\Bin\MSBuild.exe。
7-Zip：C:\Program Files\7-Zip\7z.exe。
Python：C:\Users\19812\AppData\Local\Programs\Python\Python314\python.exe。
遵循AGENTS.md；缺Python依赖直接用清华镜像安装。

## 最新候选包（没有在wj-epb部署）

完整自动恢复候选：
D:\Github\wanxiang\EPBTest\artifacts\deploy\V3.0.0.0_5d2eb9b27309_AUTO_RECOVERY_PENDING_ACCEPTANCE.7z

- 34,337,864字节，SHA256：5069D402354465A22BDB0D8B6F2A1E840D7CD01CB029E2ADE987C5F0A900A130。
- 最大压缩7z，155文件；Main+独立Guard+一键入口+清单+中文说明。
- 自动安装入口RecoverExited，RecoverStalled需额外真实验收；缺少匹配新组件身份的真实现场报告时拒绝自动安装。合成测试报告不能用于现场授权。
- 包内顶层版本目录下有Base观察安装入口；Base安装成功不表示自动恢复已开启/通过验收。

基础包：D:\Github\wanxiang\EPBTest\artifacts\deploy\V3.0.0.0_正式版_5d2eb9b27309_QUICKDEPLOY_GUARD.7z。
SHA256：5E4C31FD56BAEAC522058C80B57DB4AB05A64247D1756E16DFA1CE41D3D25396。

交付说明：D:\Github\wanxiang\EPBTest\artifacts\deploy\V3.0.0.0_5d2eb9b27309_交付与验收记录.md。
Main输出：artifacts/release-5d2eb9b/V3.0.0.0-5d2eb9b27309-20260909_000339。
Guard输出：artifacts/guard-5d2eb9b。

## 已做修复和证据边界

1. EpbDiskWriter.SealAndExportCycle原先持通道Gate执行CSV/BIN导出和校验，同设备批写需要所有通道Gate，单通道慢封存可阻塞其它通道。现在锁内领取及复制样本、SealInProgress防重复，锁外导出验证，重新锁内提交终态。导出成功之前不发布成功，不放松耐久性。
2. 生命周期读写门保护Dispose与在途seal；disposeRequested与disposed分开，失败能正确提交failed终态。RawJournal不再把后续样本纳入已冻结圈。
3. Watchdog保留可信当前PID/start绑定的ActiveRun，修复清空journal.RunId后恢复许可RunId不一致；不放松人工停止/恢复屏障。
4. 旧版2.14.2.11也有持锁Seal路径，新版FULL raw journal使负荷不同。现场缺完整在途调用栈，不得称此为唯一已证实根因，也不得保证候选包已解决所有现场问题。

已完成验证：控制805/805、磁盘76/76、电源19/19；正式Release回归通过，49项现场脚本门禁、41项部署契约通过；Guard97/97（含300次重复失败），候选准入合成6/6。

JXCQ真实SealAndExportCycle隔离回放36轮、36,000批次，2+4通道，样本数全部校验，最长批写76.472ms。不是硬件自学习。

最终同一压缩包在JXCQ中文及空格路径核对SHA256一致，Base八入口均退出0：安装、状态、修复、恢复后台、采证、启动、停止全部相关进程、卸载。监督启动窗口3.0.0.0，正常关闭成功。回退原2.17.3.0后exe哈希和ProgramData配置哈希与安装前相同，Supervisor运行、MainCount0，临时测试任务删除。

JXCQ包：C:\RecoveryGuard-3.0-Validation\学习封存 修复-5d2eb9b\package.7z。
证据：D:\Github\wanxiang\EPBTest\artifacts\field-deploy-20260908-v3-round4\jxcq-5d2eb9b-final-evidence。
其他：同目录jxcq-detached-seal-B.log、test-guard-5d2eb9b.log及构建日志。
当前无遗留构建或JXCQ测试进程需要等待。

## wj-epb最近实际事件（北京时间）

现场主机MT-20250724OGLX，项目D:\EPB_Data\10243-028，实际Current安装2.14.2.11。
原exe SHA256：9158A6CF57CFD6329D3F8DC3DD254F5E35A0D8C8B9FE48CD43B9A3919837A8BA。

- 08:34:39—41：原六通道最后持久化提交。
- 08:34:48：HydraulicSampleStale；08:34:49安全边界失败，HydraulicReleased=false、Persistence=false，随后有电源关闭命令失败。
- 08:35:27：现场既有看门狗终止旧Main19636（不是Codex），随后SupervisorSafetyHandoffIdentityMismatch反复阻断恢复。
- 09:01—09:05：I0006增量封存回传完成，见下一节。
- 用户本次确认当前安全并要求重启。持久化restart-attempt.json后，停止Supervisor排除恢复竞争，确认Main缺失且无SafetyAgent；核对PID13480/start ticks639245061477531966、路径和旧Session后精确结束残留SessionHost，恢复Supervisor。未伪造安全或许可。
- 09:07:58：现有MTTFTestAutoStart启动Main15828/start ticks639245128784768297。
- 09:09:36：普通界面开始原试验；新Session8b48f3b9bc8c47c4a7eb71b4d7882451，Run b21abf9b983144c8915db3ea0faf8bcb。
- 09:12:36：六通道4/5/7/8/9/12全部Learning→Running。
- 09:13：各完成2个正式周期后ManualStopped，ClosingFenceObserved/ManualStopRequested=true。本轮Codex未发停止命令。用户说可能误触并由其重启，此后不得自动覆盖该停止意图。
- 09:13:58界面仍存在，显示人工停止、需重启软件。此后用户重启结果尚未核实，接手必须只读重新识别，不沿用旧PID操作。

最终已知通道顺序4/5/7/8/9/12：
正式圈49058/49039/48959/48993/48980/48962；机械49852/49837/49745/49790/49777/49761；receipt_count49961/49963/49796/49842/49836/49838；最新提交09:12:54—56。

恢复只证明六通道学习通过并短时正式推进，未完成持续运行验收，不能宣布当前运行中。不要把旧版人工停止判成新的软件崩溃。

现场MTTFTestRecoveryGuard、MTTFTestRecoveryGuardExecution、MTTFTestRecoveryHealth均Disabled。它们不是本次任务要擅自开启的项目。临时CodexIncident0834UI已删除；旧CodexFieldV3UI可能仍存在且绑定旧脚本，绝不能启动。任何历史FieldUi/OpenMonitor脚本都绑定旧PID/start，必须先核对，不能盲目重用。

事件详情：docs/02_Issues/2026-09-09_0834旧版中断_只读巡检记录.md。
证据：artifacts/field-deploy-20260908-v3-round4/wj-incident0834-recovery，及wj-old-monitor-after-5d2eb9b。

## 增量备份与连接方法

现场和本地都已存在：D:\EPB_Data\Backups\10243-028_V2.14.2.11_I0006_0909_0901.7z。
大小35,925,399字节，SHA256 A34FAC24F7EE569B4966C9184149457452C898F867F15C0E95349ACC1E611CB8，现场7z测试和双端摘要校验通过。该包在本次09:09续测前创建，不包含后续新数据。
基包链ID abb1703af7f04b029b4ad21c9d85a0f8，last_sequence6；现场链D:\EPB_BackupWork\State\10243-028.backup-chain.json。增量不是独立全量备份，需保留基包及中间链。

PowerShell（本地正常配置环境）：
```powershell
$s = Get-WjEpbSession
Invoke-Command -Session $s -ScriptBlock { hostname }
Remove-PSSession $s
# JXCQ
$s = New-PSSession -ComputerName mt-20251206jxcq
```
JXCQ实际主机MT-20251206JXCQ，用户mt交互会话；wj-epb用户mt控制台。若函数缺失先检查本地PowerShell配置与章程，不猜凭据，不输出敏感信息。

只读现场快照：
```powershell
& 'D:\Github\wanxiang\EPBTest\Tools\Read-WjEpbFieldStatus.ps1' -EvidenceDirectory '<本地证据目录>'
```
该脚本旧版V1健康端点Unsupported属预期，不能据此说软件坏了。判断必须关联进程身份、新鲜心跳、学习/冷却阶段和真实SQLite提交。

备份使用远端D:\EPB_Data\EPB-IncrementalBackup_new.ps1，参数/SQLite路径参考现场章程与已完成增量脚本；远端PS5可能需仅Process作用域ExecutionPolicy Bypass。WinRM长任务保持会话，不靠断开后可能被杀掉的Start-Process子进程。只回传新增指定归档，不复制全部历史。

## 监控交接

现有heartbeat id wj-epb-24，名称wj-epb旧版续测观察，每30分钟一次，原结束时间2026-09-09T15:18:44Z（北京时间23:18:44）不延长。
本次交接由旧任务负责将这一条现有自动化target迁到新任务，不新建重复自动化。迁移结果见文末。只读；正常/人工停止/未变静默，恢复、异常、失联、结束或新的人员操作需求才通知。不擅自重启、部署、强杀、启Guard；用户接管重启的约束必须保留。

## 新任务第一批工作与完成标准

1. 读取本文、AGENTS.md、两份现场章程和最新5d2eb9b交付文档。建立接续目标；不要重做已完成构建和测试。
2. 只读核实用户自行重启后的现场版本、Main/Session/Run、人工意图、六通道阶段及真实提交。以至少两份相隔15秒的有效快照判断推进，合法学习/冷却继续等待。用户未再次授权，不发任何现场启动停止命令。
3. 核对当前候选与未完成验收差距，继续有用的本地/JXCQ工作；有证据的新问题再修复和重测，不为消耗时间重复全套验证。
4. 新版部署继续等用户明确确认；收到后按章程执行最小影响切换、增量封存回传、配置和DataStore保留、真实自学习/正式推进验证。失败优先保证现场安全与旧版试验进度，但必须遵守当时最新授权和安全事实，不盲目连续重启。
5. Guard最终应真实证明独立部署、状态快照时效、旧试验拒绝误恢复、人工停止优先、与Supervisor互斥，以及真实硬件安全边界。不能把观察模式、测试程序或JXCQ成功写成现场自动恢复验收成功。
6. 只有实际实现、正式完整包、相应验证与交付文档完成，并对未验收现场事项如实闭环，才能标记整体目标complete；I0050第二步单列，不偷换成已完成。

推荐优先阅读（都在原项目绝对路径下）：
- docs/现场远程部署与运行监控章程.md
- docs/由codex远程监控项目运行.md
- docs/外部恢复兜底检查器_实施方案.md（讨论参数不等于实现事实）
- docs/02_Issues/2026-09-09_V3学习封存导出持锁整改与验证.md（顶部和末节为最终状态，中间“正在”是历史）
- docs/02_Issues/2026-09-09_B轮恢复许可RunId不一致_分析.md
- docs/02_Issues/2026-09-09_0834旧版中断_只读巡检记录.md
- docs/RecoveryGuard_自动发布验收报告契约.md

## 交接执行结果

待创建新任务并更新本节。旧任务只完成交接，不继续与新任务并行操作现场或修改实现。
### 已完成交接

- 新任务：EPB 3.0与Guard自动恢复接续实施。
- 新任务ID：01a083cc-2e75-7472-bf76-0c87c04dc1d4。
- 新任务已回报：交接及章程已读取，接续目标已创建（active，无token预算），开始只读复核用户重启后的现场。
- 现有wj-epb-24只读监控已通过应用工具更新target到上述新任务，保持30分钟频率、原截止时间及全部停止/部署限制；未创建重复自动化。
- 旧任务原目标保留blocked历史，未冒充完成。目标内容和后续执行责任已迁交，新任务独立跟进；旧任务不再进行现场操作或实现修改。
